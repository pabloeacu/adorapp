# AdorAPP — guía para el agente

Este archivo se carga automáticamente al iniciar cualquier sesión de Claude Code en este repo. Es el contrato mínimo para no perder contexto entre sesiones.

> 📐 **Mapa completo del proyecto:** `ARCHITECTURE.md` (raíz) documenta **cada página, store, componente, librería, migración y cron** con sus funciones y flujo de datos, más la foto autoritativa de la base (tablas/RLS/crons/FKs/funciones). Leé PRIMERO este `CLAUDE.md` (el contrato: Regla de Oro + reglas + los 50 landmines), y usá `ARCHITECTURE.md` como el mapa exhaustivo del código.

## ⭐ REGLA DE ORO — método obligatorio para CADA pedido (innegociable)

Aplica a **toda** solicitud de Paul, **sin importar cuán chica o trivial parezca**. No es opcional ni por caso: es el estándar permanente del proyecto. Vale para esta sesión y para cualquier sesión futura.

**Cada chunk arranca por el panorama y cierra por el informe.** Antes de tocar código: analizá TODO el panorama, evaluá todas las alternativas posibles y traé sugerencias, correcciones o preguntas ANTES de avanzar. La plataforma está **EN VIVO con usuarios reales**, así que cada cambio se hace con **precisión quirúrgica** y con la garantía absoluta de no romper nada. Al terminar, cerrá con un **informe breve en lenguaje sencillo** que le permita a Paul entender que todo se hizo según lo acordado y, si quedara algo, qué queda pendiente y por qué. (Confirmado y reforzado por Paul el 2026-09-05: es innegociable.)

1. **Alcance total, nunca aislado.** Lo que Paul reporta es el *síntoma*, no el límite del trabajo. Pensá transversalmente: investigá TODO lo que pueda estar relacionado (misma causa raíz, mismo patrón, mismo componente, mismos datos, mismas capas). Resolvé el problema **y toda su familia**, aunque no esté denunciado.
2. **Todas las hipótesis, incluso las remotas y descabelladas.** Enumerá causas posibles antes de decidir. No descartes una por improbable sin evidencia. La causa real suele no ser la obvia.
3. **No des por sentado que algo funciona: probalo EN VIVO.** Nada se declara resuelto ni "ya andaba" sin verificación empírica. Reproducí el bug (control negativo) y validá el fix en un navegador real (Chromium/Playwright, gestos táctiles por CDP si aplica), contra prod cuando corresponda, y en la base de datos real (QA transaccional con `ROLLBACK`). jsdom no alcanza para timing/scroll/DnD/history. **Nunca alcanza con mirar tablas o lógica: hay que ejecutarlo.**
3-bis. **Pruebas destructivas, no solo de cobertura.** El testeo no busca únicamente "no dejar cabos sueltos": es **adversarial** — pone a prueba la seguridad, la robustez y la certeza absoluta del sistema. Intentá ROMPERLO a propósito: escalar privilegios, saltar RLS, doble-submit / carreras, inputs hostiles, roles equivocados, datos corruptos, actores no autorizados. Un cambio recién se declara sólido cuando resistió el ataque, no cuando pasó el caso feliz. Para trabajo de seguridad, desplegá una auditoría con subagentes (Workflow) que red-teamee el diseño y barra la familia entera de huecos.
4. **Auditoría doble y triple, a fondo.** Después de arreglar, barré el resto del código buscando el mismo patrón en otros lugares (grep del anti-patrón), y verificá que no rompiste nada de lo que ya funcionaba. Garantía **100%** de no-regresión.
5. **Optimización en el camino.** Cazá cada detalle y falla que cruces aunque no sea el pedido — corregí o registralo. Dejá el código mejor de como lo encontraste.
6. **Cero intervención de Paul.** Desplegá los agentes/subagentes que hagan falta, usá el navegador, los MCP (Supabase, Vercel, GitHub, Chrome), instalá herramientas (ffmpeg, playwright-core) — lo que sea necesario para obtener el resultado **vos**. Paul no ejecuta pasos manuales ni valida por vos.
7. **Sin excusas, sin reserva de esfuerzo ni talento.** Certeza completa de la resolución con alcance total antes de cerrar. Si algo no se puede garantizar, decilo explícito y por qué — pero primero agotá todo.

> Guía práctica ya probada en este repo: reproducir en Chromium con `playwright-core` + el Chromium de `/opt/pw-browsers` (ver `scratchpad/run_*.mjs`); QA de DB con `BEGIN … ROLLBACK` impersonando roles; PR por parte con CI verde + smoke prod; y auditar el anti-patrón con `grep -rn` en todo `src/`.

## Qué es esto

PWA en Vite/React 18 + Supabase + Vercel para ~8 usuarios reales del **ministerio de adoración de Adoración CAF**. Los líderes arman **órdenes** (la lista de canciones de cada reunión/culto). Pastores son Paul y Ana.

- **Dominio prod:** https://adorapp.net.ar (Vercel project `adorapp`, region `gru1`).
- **Repo:** github.com/pabloeacu/adorapp (público, branch `main` protegida).
- **Backend:** Supabase plan **Pro**.
- **Working dir:** este directorio (`adorapp/`). La carpeta padre `Desktop/Adorapp/` es un wrapper con scripts viejos — no operes desde ahí.

## Reglas no negociables

1. **No commitees a `main` directo.** Trabajá en ramas con prefijo (`audit/`, `fix/`, `perf/`, `refactor/`, `feat/`, `docs/`).
2. **Antes de cualquier acción destructiva o irreversible** (drop tablas, cambiar RLS prod, modificar DNS, rotar secrets, eliminar branches Vercel), pará y pediendo confirmación con el cambio exacto.
3. **No le pidas a Paul pasos manuales en Supabase, Vercel o GitHub.** Usá los MCPs (`mcp__c5073f58-...` para Supabase, `mcp__a9edc114-...` para Vercel, `gh` para GitHub) o tomá control de Chrome (`mcp__Claude_in_Chrome__*`).
4. **Cierre narrativo no-técnico al terminar cada fase.** "Qué pasaba → qué hice → qué cambia para vos como pastor". El detalle técnico va al `AUDIT_LOG.md` y a los commits.
5. **Testeá que no se rompa lo que funciona** antes de cerrar una fase: lint + build + tests + smoke en prod.
6. **Sin `service_role` en cliente.** Las operaciones privilegiadas pasan por Edge Functions admin-* (ya hay 7 desplegadas).
7. **Toda migración que `CREATE TABLE` en `public` debe incluir explícitamente `GRANT SELECT, INSERT, UPDATE, DELETE ON <tabla> TO authenticated;`** (y `TO anon` si corresponde). Desde el 30-oct-2026 Supabase deja de exponer tablas nuevas al Data API por defecto; este paso anticipa el cambio y evita "tabla creada que el cliente no puede leer".
8. **NUNCA llamar `supabase.from(...).update(convertXToDB(partial))`.** Los `convertXToDB` en `src/stores/appStore.js` generan rows completos con defaults para INSERTs. Si un partial pasa por ahí, Postgres SOBRESCRIBE TODA la fila con esos defaults — pérdida silenciosa de letra/acordes/tono/etc. Siempre rutear vía `updateMember/Band/Song/Order` del store, que mergean el partial con el snapshot del store antes del converter. Incidente raíz: 15-jun-2026, PR #20. Si agregás una nueva tabla con su propio converter, replicar este patrón (merge primero) y dejar el comentario "DATA-LOSS LANDMINE" sobre el converter.

## Vocabulario eclesial (importa)

- "**orden**" (no "culto") = lista de canciones de la reunión. **Es MASCULINO** ("*el* orden de las canciones", "*un nuevo* orden", "*primer* orden", "orden elimina*do*/duplica*do*/programa*do*"), NO femenino ("~~la orden~~" = mandato/pedido). Toda concordancia en el texto visible (artículos, adjetivos, participios, pronombres "lo/los") va en masculino. Los `label` de estado y los filtros ya están así (Programado/Completado/Cancelado). Corregido en PR #52 (commit `61eb133`); si agregás copy nuevo sobre órdenes, mantené el masculino.
- "**ministerio de adoración**" (no "iglesia entera") cuando hablás del alcance de comunicación.
- "**estándares de seguridad**" (no "seguridad de tipo empresa").
- "hecha **a medida** del ministerio" (no "única en Hispanoamérica").
- Cierre canónico para presentaciones a pastores generales: *"siguiendo la visión de los pastores generales de la iglesia, Claudio y Claudia Tomaselli, perseguimos la excelencia con el único afán de que brille el Rey y se extienda Su reino"*.

## Estética

- Negro plano queda simplón. Preferí gradientes radiales aurora (azul/violeta) sobre fondos oscuros.
- Para PDFs e iOS: nunca confiar en `radial-gradient + mask-image` — generá PNG real con `scripts/gen-aurora-bg.cjs` y embebelo.
- Logos PNG sobre fondo dark: `mix-blend-mode: lighten` para eliminar el cuadrado negro.
- Spinners de carga: usar `<PageLoader />` (logo + pulso + "Cargando…"), no spinners circulares genéricos.

## Stack y archivos clave

- `src/App.jsx` — routing con React.lazy por ruta.
- `src/stores/{authStore,appStore}.js` — Zustand. `authStore.refreshProfile()` dispara `appStore.initialize()`.
- `src/lib/supabase.js` — cliente público, `callAdminFunction()` para Edge Functions.
- `src/lib/{csv,orders}.ts` — únicos archivos TS hasta ahora; resto del código es JS.
- `src/components/layout/{Header,MobileNav}.jsx` — los dos archivos monstruo (1712 + 1242 líneas, ~40% duplicado). Refactor pendiente en backlog.
- `src/components/ui/PageLoader.jsx` — loader unificado.
- `supabase/` — migrations + edge functions.
- `presentation/` — generador del PDF para pastores generales (`render.cjs` + `render-mobile.cjs`).
- `scripts/seed-devotionals.cjs` — fuente de verdad para los 365 versículos RV60.
- `AUDIT/` — auditoría inicial; ver `AUDIT_LOG.md` y `00_REPORT.md`.
- `docs/RUNBOOK.md` — rollback, restore PITR, rotación de keys, recuperación cron.

## Crons activos (Supabase pg_cron)

- `daily-devotional-notification` 06:00 ART
- `daily-reflection-notification` 17:00 ART
- `reflection-monitor` cada 6 h (escribe a `error_log` si falta reflexión >25 h)
- `daily-birthday-notification` 09:00 ART (push a pastores con cumpleaños del día; jobid 7)
- `rehearsal-reminders` cada 15 min (push 2 h antes del ensamble programado en un orden a los **participantes** del orden — `order_participant_ids`; ver "Estado al 2026-06-20" y "Estado al 2026-09-11 (II)")
- `practice-reminders` `0 21 * * *` (18:00 ART): alarma personal de ensayo (opt-in) a quien **participa** de ≥1 orden programado con canciones y Ensayómetro < 100%. Ver "Estado al 2026-08-03 (II)" y "(II) del 2026-09-11".
- `practice-cleanup` `30 7 * * *` (04:30 ART): poda `practice_logs` de órdenes no programados con fecha < hoy−7.
- `collab-expire` 04:00 ART: vence solicitudes de colaboración abiertas de órdenes pasados.
- `auto-complete-orders` `0 6 * * *` (03:00 ART): pasa a `completed` las órdenes `scheduled` con `date < hoy ART` (no toca canceladas; UPDATE no dispara push). Ver "Estado al 2026-06-21".
- `leader-activity-digest` `0 1 * * *` (22:00 ART): resumen diario a los pastores (SOLO correo) de lo que hicieron LÍDERES o miembros EDITORES sobre songs/orders/bands (+ altas de integrantes temporales) en las últimas 24 h; solo si hubo movimientos. Clasifica por los CAMPOS que cambiaron de verdad (`activity_digest_items`): las alertas ⚠️ (tonalidad/acordes de canciones que ya existían) van primero; armar un orden o transportar dentro de un orden NO cuenta como edición. Ver "Estado al 2026-09-06" y "Estado al 2026-09-10".

## Comandos útiles

- `pnpm dev` (o `npm run dev`) — dev server.
- `npm run lint` / `npm run lint:fix`.
- `npm test` — Vitest.
- `npm run build` — build de prod (CI lo corre + integrity grep contra `service_role`/`supabaseAdmin`).
- `node presentation/render-mobile.cjs` — regenerar PDF mobile de la presentación.
- `node scripts/gen-aurora-bg.cjs` — regenerar imagen aurora base.
- `node scripts/seed-devotionals.cjs` — regenerar SQL de devocionales.

## Estado al 2026-04-28

Fases A, B, C, D, E1 cerradas en producción. Última sesión grande quedó clavada por capturas >2000 px acumuladas; el contexto se preservó en `~/.claude/projects/-Users-paulair-Desktop-Adorapp/memory/`. Untracked al cierre: `AdorAPP-PresentacionFinal-Movil.pdf`, `scripts/gen-aurora-bg.cjs`. Hilo abierto: Paul reportó "tengo un tema con la notificación de hoy" — preguntar de qué se trataba al retomar.

## Estado al 2026-05-16

Sesión grande, 5 PRs cerrados a producción + validados online con 3 roles reales:
- PR #14 ícono CAF en home screen (commit `5f26c81`).
- PR #15 paso 5 del wizard "guardar como app" (commit `378a945`). Wizard ahora tiene 5 pasos: Bienvenida → Tour → Datos → Notifs → Instalar.
- PR #16 visibilidad por rol en /miembros (commit `f5ac2b6`): líder ve sólo nombre/rol/instrumentos; miembro no ve la sección + route guard `<MembersOnlyRoles>` en `App.jsx` redirige a `/`.
- PR #17 push diario cumpleaños 09:00 ART para pastores (commit `0f0999e`): `send_daily_birthday_notifications()` + cron job 7 + tipo `'birthday'` + ícono Cake rosa.
- PR #18 mobile bottom nav 4 fijos + hamburguesa secundaria (commit `0316347`): pastor ve 3 ítems en hamburguesa, líder sólo Miembros, miembro sin hamburguesa.

**Operativa nueva:**
- Mergeo de PRs vía GitHub API REST con PAT del Keychain (`security find-internet-password -s "github.com" -w`). Sin `gh` CLI.
- Migrations a Supabase prod vía MCP `apply_migration` (proyecto AdorAPP id `gvsoexomzfaimagnaqzm`).
- Tests E2E con Claude in Chrome MCP contra prod, leyendo DOM del MobileNav vía `javascript_tool` (Tailwind `lg:hidden` no renderiza visualmente pero el HTML está, perfecto para auditoría).

**Regla importante para no romper:** NUNCA crear test users vía SQL crudo. Genera error "Database error querying schema" porque se saltea `auth.identities` + inicialización de tokens como ''. Usar siempre `admin-create-member` EF (misma puerta que el UI del pastor). Detalle completo en `memory/project_state_20260516.md`.

## Estado al 2026-06-18

Sesión "mobile" grande, **7 PRs a producción** (rama `claude/mobile-photo-upload-3asq58`), todos validados en vivo por Leandro (líder) en su celular real. Foco: que la versión móvil tenga paridad y funcione de punta a punta. CI verde (lint+build+28 tests+smoke prod) en cada merge.

- **PR #24** (`91963bc`): (a) **foto de perfil no guardaba** → el bucket `avatars` tenía RLS habilitado con **CERO políticas**; se agregaron 4 (ver "Subsistema avatars"). (b) **contenido tapado por la barra inferior** → `Layout.jsx` mobile usaba `pb-16` sin contemplar `env(safe-area-inset-bottom)`; ahora `paddingBottom: calc(80px + env(safe-area-inset-bottom))`.
- **PR #25** (`3dacc10`): botón **"Imprimir"** (PDF de canciones con acordes, `generateSongsPDF`) inalcanzable en celular → la fila de acciones de la orden se desbordaba sin `flex-wrap`. Header de tarjeta apila en móvil + `flex-wrap`. También se agregó "Imprimir" en la vista de detalle.
- **PR #26** (`ebf4a3b`): **auditoría completa de paridad web↔móvil** (HIGH+MEDIUM+LOW). Lo más grave: en /miembros las acciones del pastor (resetear pass/editar/eliminar) estaban `opacity-0 group-hover` → **invisibles al tacto** (ahora `opacity-100 lg:opacity-0 lg:group-hover:...`). Además: botón búsqueda del Header desktop estaba muerto (sin onClick); "Eliminar foto" faltaba en móvil; tablas con `min-w`+columnas ocultas en móvil; `flex-wrap` en clusters varios; grids responsive; "Sincronizar" en MobileNav; email/rol en perfil móvil; limpieza de código muerto. **Verificado OK:** navegación por rol Sidebar==MobileNav (idéntica) y el `<Modal>` compartido ya era mobile-safe.
- **PR #27** (`9413711`): **los modales no se cerraban en celular** (el gesto "atrás" navegaba de sección) → el `<Modal>` compartido ahora se integra con el historial (`pushState` al abrir, `popstate`/back → cierra) y se renderiza con `createPortal` a `document.body`. Cabecera con botón "Cerrar" visible. **Beneficia a TODOS los modales.**
- **PR #28** (`ec09740`): botón **Guardar** del recortador quedaba bajo el notch/status bar → se movió a una **barra inferior fija** ("Guardar cambios") con `env(safe-area-inset-bottom)`, y la barra superior respeta `env(safe-area-inset-top)`.
- **PR #29** (`d753f79`): **el guardado de foto era un no-op silencioso en móvil** → `handleSavePhoto` leía `fileInputRef.current?.files?.[0]` y salía si era null; pero ese `<input>` vive dentro del modal "Cambiar foto" que se **desmonta** al abrir el recortador → `fileInputRef.current` null → return. Fix: el guardado dibuja desde `previewUrl` (que persiste), no del input.
- **PR #30** (`3f0b983`): **el recorte guardado no coincidía con la vista previa** (bug recurrente histórico) → el guardado adivinaba el tamaño con constantes (200/280) que no coincidían con el render real, y la preview centraba con `margin: -50%` irreproducible. Fix: preview con `transform: translate(-50%,-50%)`; el guardado **mide `cropImgRef.current.offsetWidth/Height`** (tamaño real renderizado) y replica el pipeline exacto en canvas (`center → scale(k) → scale(zoom) → rotate → translate(px/zoom,py/zoom) → drawImage` centrado, `k=canvas/circle`).
- **PR #32** (`1a2daaf`): (a) en los modales de formulario el **footer tapaba el contenido** → en `Modal.jsx` el footer pasó de `absolute bottom-0` a **hijo flex `shrink-0`** (content `flex-1` scrollea entre header y footer; se quitó el `pb-32`). (b) **'Nueva Orden' recordaba la canción tipeada** al reabrir → `handleOpenModal`/`handleCloseModal` de `Ordenes.jsx` blanquean `songSearchTerm`/`showSongDropdown`/`keyHistoryTooltip` (vivían fuera de `formData`); `Repertorio.jsx` cierra su dropdown de categorías al cerrar. Auditado: Bandas/Repertorio/Miembros ya reseteaban `formData` en `handleOpenModal`.

**Subsistema avatars (importante):**
- Bucket `avatars` es **público**, mime `image/png|jpeg|gif|webp`, límite 5MB. Hasta el 18-jun-2026 tenía RLS ON sin políticas → ningún upload funcionaba para nadie. Políticas actuales sobre `storage.objects`: `avatars_public_read` (SELECT public), `avatars_authenticated_insert/update/delete` (TO authenticated, `bucket_id='avatars'`). Migración `supabase/migrations/20260618_avatars_storage_rls.sql`.
- La foto de Paul es un **data-URI base64 embebido en `members.avatar_url`** (~58k chars) de antes de migrar a Storage. Funciona pero infla la fila. Backlog: migrarla a Storage.
- Flujo móvil de foto vive en `MobileNav.jsx` (`handleCameraClick`→modal `showPhotoModal`→`handleFileSelect`→recortador `showCropper`→`handleSavePhoto`). El desktop es **otro** cropper en `Header.jsx` (duplicado ~40%).

**Landmines nuevos (no re-romper):**
1. **Cropper móvil**: `handleSavePhoto` NO debe depender de `fileInputRef` (el input se desmonta al cerrar el photo-modal). Dibujar siempre desde `previewUrl`.
2. **Cropper accuracy**: para que el recorte guardado == preview, medir el tamaño REAL del `<img>` (`offsetWidth/Height`, no afectado por transform) y replicar el MISMO pipeline de transform. No usar constantes de tamaño.
3. **Acciones touch**: nunca esconder acciones con `opacity-0 group-hover` sin un `lg:` que las deje visibles en móvil (no hay hover en touch).
4. **Filas de botones**: usar `flex-wrap` en clusters de acciones dentro de `justify-between`; en pantallas angostas se recortan fuera de la tarjeta.
5. **Bucket nuevo público**: además del GRANT de tablas (regla #7), un bucket de Storage necesita sus **políticas RLS explícitas** sobre `storage.objects` o nadie puede subir.
6. **`<Modal>` compartido** (`src/components/ui/Modal.jsx`): ahora usa history+portal. Si tocás modales, el back gesture los cierra; no agregar cierre por backdrop (rompería forms con pérdida de input).
7. **Footer del `<Modal>`**: es un hijo flex `shrink-0` (NO `absolute`), si no se superpone al contenido y tapa los últimos campos. El contenido es `flex-1 overflow-y-auto`; no volver a meterle `pb-32` ni footer absoluto.
8. **Reset de formularios**: `handleOpenModal` resetea `formData`, pero el estado de búsqueda/dropdown que vive FUERA de `formData` (ej. `songSearchTerm` en Ordenes) hay que blanquearlo aparte en open/close o el modal "recuerda" lo tipeado al reabrir.

**Operativa:** PRs creados y mergeados (squash) vía GitHub MCP; CI gated por branch protection en `main` (workflow "CI": Lint+Build+Test + Smoke test prod). Validación visual la hace el usuario en su celular (sin Claude-in-Chrome en este entorno; Leandro no puede abrir previews de Vercel si tienen protección). Auditoría de paridad detallada: `AUDIT/06_parity.md`.

**Backlog abierto:**
- El cropper desktop (`Header.jsx`) tiene la **misma matemática frágil con constantes** que tenía el móvil (no se tocó; el reporte fue sólo móvil). Aplicar el mismo fix de medición si aparece el problema en compu.
- Extraer el cropper a un hook/componente compartido (hoy duplicado Header+MobileNav ~40%).
- Convertir los `alert()` del perfil móvil a modales (UX; en desktop ya son modales).
- Migrar la foto base64 de Paul a Storage.

## Estado al 2026-06-20

Dos cosas: (a) fix de modales de formulario (PR #32, ya arriba) y (b) **feature nueva "Programar ensayo"** (PR #34, commit `2053b19`).

**Programar ensayo (por orden):** en "Nueva Orden" un switch **"Programar ensayo"** habilita día + hora. Si se programa:
- **Push 2 h antes** a todos los integrantes de la banda el día del ensayo.
- **Evento en el calendario** ("Ensayo · banda", ámbar) ligado a la orden.
- **Card en el inicio** ("¡Hoy tenés ensayo!", amarillo, full-width) visible el día del ensayo 08:00–23:00 ART, con link directo `/ordenes?order=<id>`.

**Implementación:**
- DB (`supabase/migrations/20260620_rehearsal_reminders.sql`): columnas aditivas nullable en `orders` (`rehearsal_date date`, `rehearsal_time text` 'HH:MM', `rehearsal_reminder_sent bool` dedup). Función `send_rehearsal_reminders()` (SECURITY DEFINER, `search_path` fijo, **REVOKE** del RPC como las otras de cron) + cron `rehearsal-reminders` `*/15 * * * *`. En la ventana `[ensayo-2h, ensayo)` ART, una sola vez (dedup flag), inserta 1 notificación `'reminder'` por integrante activo con cuenta → dispara el push existente (trigger `notify_push_on_notification_insert`).
- `appStore.js`: `rehearsalDate/Time` en `convertOrderFromDB/ToDB`. **`rehearsal_reminder_sent` NO se escribe desde el cliente** (lo maneja sólo el cron).
- `Ordenes.jsx`: switch + campos; reset en open; submit adjunta ensayo sólo si está activo; lee `?order=` y abre el detalle.
- `OrderCalendar.jsx`: bucket `rehearsalsByDay` + pill ámbar. `Dashboard.jsx`: card con cálculo de hora ART vía `toLocaleString`.

**QA hecho (en prod, sin push real):** la función se probó dentro de `BEGIN; SET LOCAL session_replication_role=replica; … ROLLBACK;` (triggers off + rollback = doble blindaje, cero push). Verificado: inserta 1 notif por miembro (3 en Banda Test), marca dedup, 2º llamado no duplica, ventana 2h OK, sin residuos, y alta de orden normal sigue andando. Advisors de seguridad: 0 alertas nuevas (la función no aparece en `function_search_path_mutable` ni en `authenticated_security_definer_function_executable`).

**Landmines nuevos:**
9. **`orders.rehearsal_reminder_sent`**: lo escribe SÓLO el cron. Nunca incluirlo en `convertOrderToDB` (si no, una edición de orden lo pisaría y re-enviaría el push).
10. **Probar funciones que insertan en `notifications`**: el INSERT en `orders`/`bands`/`notifications` dispara triggers que mandan push global/real. Para QA, SIEMPRE `BEGIN; SET LOCAL session_replication_role = replica; … ROLLBACK;` — desactiva triggers (no encola push) y revierte. Nunca correr `send_rehearsal_reminders()` "en vivo" contra una banda con miembros reales fuera de ese envoltorio.
11. **Fecha+hora del ensayo como `date` + `text`** (no `timestamptz`): evita el off-by-one de TZ en el calendario/dashboard (un `timestamptz` 22:00 ART cae en el día UTC siguiente). El cron combina ambos en ART.

## Estado al 2026-06-21

**Órdenes — 3 mejoras (PR #36, commit `bad2e06`):**
- **Editar orden:** se reutiliza el modal de "Nueva Orden". `handleOpenModal(order)` precarga `formData` + setea `editingOrder`; `handleSubmit` rutea a `updateOrder` (merge-safe) en vez de `addOrder`. Botón "Editar" en tarjeta y detalle (pastor/líder). `saveKeyHistory` es upsert (`onConflict member_id,song_id`) → editar no duplica historial.
- **Control de estado manual** en el detalle (pastor/líder): Marcar completada / Cancelar / Reabrir, vía `updateOrder({status})` + patch del snapshot `viewingOrder`.
- **Auto-completar pasadas (DB):** `auto_complete_past_orders()` + cron `auto-complete-orders` `0 6 * * *`. UPDATE `scheduled → completed` donde `date < hoy ART`. No toca canceladas; UPDATE NO dispara push (el trigger de orden es sólo INSERT). Blindada del RPC (REVOKE). Migración `20260621_auto_complete_past_orders.sql`. Corrida una vez al aplicar (la orden del 16-jun pasó a completada → el contador "Servicios completados" del Dashboard ya tiene sentido).
- **Filtro por defecto de Órdenes:** `all → scheduled` (`Ordenes.jsx`), como Solicitudes con `pending`. Evita la lista infinita de pasadas.

**Landmines nuevos:**
12. **`handleOpenModal(order = null)`** en Ordenes: los botones "Nueva Orden" DEBEN llamar `() => handleOpenModal()` — si pasan el handler directo, React les manda el evento como `order` y abren en modo edición con basura. (Mismo patrón si se reusa el modal para editar en otras pantallas.)
13. **Editar orden NO re-arma el recordatorio de ensayo:** `convertOrderToDB` no escribe `rehearsal_reminder_sent` (landmine #9), así que si reprogramás el ensayo de una orden cuyo flag ya está `true`, el cron no vuelve a avisar. Caso borde conocido (los ensayos se setean casi siempre al crear). Si hiciera falta, resetear el flag server-side, no desde el cliente.

## Estado al 2026-07-01

**Exportar PDF fallaba al transportar el tono (PR #38, commit `db1e818`).** Reportado por Daniel (Córdoba): exportar anda en tono original, "no funciona" al transportar.

- **Causa real (NO era un crash):** reproduje `generateSongPDF` en Node con jsPDF + datos reales → genera OK en todos los tonos. El "no descarga" era el **manejo async**: el botón "Descargar PDF" tenía `try/catch` **síncrono** alrededor de una función **async**, dentro de un `setTimeout` que cerraba el modal antes → cualquier rejection tras el primer `await` se tragaba en silencio. Fix: handler `async` + `await generateSongPDF()` con catch real + cerrar el modal DESPUÉS de disparar la descarga (se quitó el setTimeout).
- **Bug de correctitud (motor de transposición):** el regex de acorde exigía raíz MAYÚSCULA `[A-G]`, así que un `c9` en minúscula (typo, presente en "A Ti Me Rindo") NO se transportaba y salía mal en cualquier tono. Fix en `appStore.js`: `[A-Ga-g]` + `toUpperCase` en raíz y bajo. Motor compartido → beneficia viewer, PDF Repertorio y PDF Órdenes.
- **Preventivo:** los 4 botones de PDF de Órdenes se llamaban fire-and-forget → se envuelven en `runPdfExport()` que surfacea el error. Test nuevo `src/stores/transpose.test.js` (7 casos) fija el motor de transposición.

**Landmines nuevos:**
14. **PDF export es `async`:** nunca envolver `generateSongPDF/generateSongsPDF/generateOrderPDF` en un `try/catch` síncrono ni dispararlas en `setTimeout` — el rejection se traga y el usuario ve "no pasa nada". Usar `await` con catch, o `runPdfExport()` (Ordenes). Generar ANTES de cerrar el modal.
15. **Acordes en minúscula:** el motor de transposición ahora acepta raíz `[A-Ga-g]` y la pasa a mayúscula. Si tocás el regex de `transposeChordToken`, mantené el case-insensitive o los typos tipo `c9` dejan de transportar. Cubierto por `transpose.test.js`.

## Estado al 2026-07-10

**Revisión de seguridad + rate limiting del registro público (PR #40, commit `b7d124e`).** Paul trajo un reel de un influencer con 6 consejos de seguridad (RLS, CORS, security headers, rate limiting, API keys en `.env`, anti SQL-injection) y pidió asegurar que la plataforma esté "blindada".

- **Auditoría (sin código):** 5 de los 6 puntos ya estaban sólidos —RLS en las 16 tablas públicas; headers fuertes en `vercel.json` (HSTS preload, CSP, X-Frame-Options DENY, nosniff); `service_role` sólo server-side (CI lo grepea); sin secretos filtrados; sin SQL crudo con input de usuario (todo via PostgREST/RPC parametrizado); CORS `*` en `send-push` es seguro porque no usa credenciales, sólo token. El único hueco real era **rate limiting**.
- **Rate limiting (lo único que Paul aprobó implementar):** el formulario público "solicitar registro" (`pending_registrations`) es el único lugar donde `anon` puede escribir por diseño. Sin freno, un bot podía inundarlo. Se agregó un trigger `BEFORE INSERT` `rate_limit_pending_registrations()`: **máx 10 solicitudes/min** (global) + backstop de **200 pendientes**; ambos devuelven **HTTP 429** vía `PT429` (convención PostgREST) con mensaje amable en español. `Login.jsx` mapea `code === 'PT429'` a un mensaje claro. Umbrales generosos: un ministerio de ~8 nunca los toca, un flood sí. Migración `supabase/migrations/20260710_rate_limit_pending_registrations.sql`.
- **QA (prod, sin push real):** función `SECURITY DEFINER` con `search_path` fijo + `REVOKE EXECUTE` de PUBLIC/anon/authenticated (patrón blindado). Test transaccional (`BEGIN … ROLLBACK`, deshabilitando SÓLO el trigger de push `notify_on_pending_registration_insert`, no todos): 10 inserts pasan, el 11º se bloquea con PT429; sin residuos, triggers restaurados. `get_advisors` security: 0 alertas nuevas (la función no aparece en `function_search_path_mutable` ni en `authenticated_security_definer_function_executable`).

**Landmines nuevos:**
16. **Rate limit del registro:** el límite es un trigger `BEFORE INSERT` sobre `pending_registrations`, no lógica de cliente (el cliente no se puede confiar). Si subís el volumen real del ministerio, subí el umbral de 10/min en la función. El `RAISE … USING ERRCODE='PT429'` es lo que hace que PostgREST devuelva 429; si cambiás el código, actualizá el `if (insertError.code === 'PT429')` de `Login.jsx` o el usuario ve un error genérico.
17. **QA de triggers en `pending_registrations`:** el INSERT dispara `notify_on_pending_registration_insert` (push a pastores). Para probar el rate limit NO se puede usar `session_replication_role=replica` (apagaría también el trigger que querés probar). Deshabilitar SÓLO el trigger de push con `ALTER TABLE … DISABLE TRIGGER notify_on_pending_registration_insert` dentro del `BEGIN … ROLLBACK` (el ROLLBACK revierte el DISABLE).

## Estado al 2026-07-20

Dos fixes reportados por líderes reales, ambos a producción en la rama `claude/mobile-photo-upload-3asq58`.

**(a) Eliminar orden — líderes sin botón + borrado no-op (PR #42, commit `d19cffd`).** Reportado con capturas: (1) los líderes no veían "Eliminar" y (2) el borrado "no hacía nada" ni como pastor.
- **Bug 1 (dos capas):** el botón en `Ordenes.jsx` estaba gateado `{isPastor && …}` **y** la política RLS de DELETE era `is_pastor()` sola → el líder quedaba bloqueado en la base aunque viera el botón. Fix: UI `(isPastor || isLeader)` + política `orders_delete_pastor_or_leader` (`is_pastor_or_leader()`).
- **Bug 2 (no-op silencioso):** la FK `song_key_history.order_id → orders.id` estaba en `NO ACTION`; cada orden guarda tonalidades al crearse/editarse, así que borrar una orden con historial lanzaba `23503` (violación de FK). El handler llamaba `deleteOrder()` **fire-and-forget** y mostraba "eliminada" igual. Fix: FK a `ON DELETE SET NULL` (conserva la memoria de tono, suelta la referencia) + el handler ahora hace `await deleteOrder()` y muestra un `ErrorModal` real si falla. Migración `20260720_fix_order_delete_leader_and_fk.sql`. QA transaccional (BEGIN…ROLLBACK, impersonando roles con `set_config('request.jwt.claims',…)`): pastor y líder borran 1 fila, miembro 0; `song_key_history` pasa de 8→8 (no se pierde ninguna), las de la orden quedan con `order_id` null.

**(b) "Cambiar Contraseña" no abría desde "Mi Perfil" en escritorio (PR #43, commit `0d838ef`).** Reportado por Daniel Córdoba (video): clic en "Cambiar Contraseña" cerraba el menú y volvía al inicio, sin abrir el formulario.
- **Causa:** el `<Modal>` compartido integra el historial (pushState al abrir; back → cierra). El botón hace `setShowPasswordChange(true)` + `setShowProfile(false)` a la vez; al cerrarse el modal de Perfil, su cleanup llamaba `history.back()`, cuyo `popstate` era capturado por el listener del modal de Contraseña **recién montado**, cerrándolo al instante.
- **Fix (en `Modal.jsx`):** un **único** listener de `popstate` a nivel de módulo + una **pila** de modales abiertos + un **contador de backs programáticos**. Cuando un modal que se cierra saca su entrada dummy con `history.back()`, ese `popstate` se absorbe con el contador en vez de tratarse como un back del usuario. El back gesture real sigue cerrando el modal top. El modal de contraseña **móvil** (MobileNav) usa un `<div>` propio sin historial → no sufría el bug.
- **Verificación:** jsdom NO reproduce el timing de `history.back()` de un navegador real, así que se validó el `Modal.jsx` real en **Chromium (Playwright)** con control negativo: código anterior → el modal de contraseña se cierra solo; con el fix → permanece abierto y el back real lo cierra.

**Landmines nuevos:**
18. **`song_key_history.order_id` es `ON DELETE SET NULL`** (no `NO ACTION`, no `CASCADE`): borrar una orden conserva la "memoria de tono" por (miembro, canción) y sólo suelta la referencia. `order_id` es nullable y `fetchKeyHistory` tolera null (muestra el tono con "fecha no disponible"). No volver a `NO ACTION` o el borrado de órdenes con historial rompe con `23503`.
19. **DELETE de `orders` es `is_pastor_or_leader()`** (línea con insert/update). Si tocás la visibilidad del botón "Eliminar" en `Ordenes.jsx`, mantené el gate `(isPastor || isLeader)` alineado con la RLS. El handler de borrado DEBE `await deleteOrder()` y ramificar (éxito/error) — nunca fire-and-forget (mismo patrón que landmine #14 de PDFs).
20. **`<Modal>` usa historial GLOBAL** (`src/components/ui/Modal.jsx`): hay UN listener de `popstate` a nivel de módulo + pila + contador `programmaticBackPending`. NO volver a un listener `popstate` por-instancia: rompe cualquier transición modal→modal (abrir B cerrando A, ej. Perfil→Cambiar Contraseña), porque el `back()` del que se cierra cierra al recién abierto. Para testear esta lógica NO sirve jsdom (no modela el `popstate` async del navegador); validar en Chromium real.

## Estado al 2026-07-25

**Sheet "Mi Perfil" móvil no scrolleaba en algunos celulares (PR #44, commit `5b003d7`).** Reportado con captura: varios usuarios veían el perfil cortado y sin poder scrollear → no llegaban a "Activar notificaciones", "Cambiar contraseña" ni "Cerrar sesión" (todo debajo de "Sincronizar"). A otros les andaba → dependía del navegador/celular.
- **Causa:** el contenedor scrolleable del sheet (`MobileNav.jsx`, `max-h-[90vh] overflow-y-auto`) tenía `touch-action: none` en el `style`, que en varios navegadores/versiones **bloquea el scroll táctil** aunque haya `overflow-y-auto`. El sheet NO tiene gesto de arrastre propio (el `ref` no engancha touch/drag), así que ese `touch-action:none` era un remanente sin función.
- **Fix:** quitar `touch-action:none` (scroll táctil por defecto) + `overscroll-behavior:contain` (evita scroll chaining al fondo). Cambio **estrictamente aditivo** (none→auto sólo agrega scroll, no afecta a quienes ya veían bien).
- **Verificación:** Chromium real (Playwright, viewport móvil + `hasTouch`) con swipe táctil sintetizado por CDP (respeta `touch-action`): con `none` el scroll queda en 0 y "Activar notificaciones" oculto; con el fix scrollea hasta el fondo y la opción queda visible.

**Landmines nuevos:**
21. **Bottom-sheets móviles scrolleables (`overflow-y-auto`) NO deben llevar `touch-action: none`** — bloquea el scroll táctil en algunos navegadores/celulares. `touch-action:none` es SÓLO para superficies con gesto de arrastre propio (el recortador de foto en `MobileNav.jsx` y `Header.jsx`, que sí lo necesitan para pan de la imagen). Para un sheet que scrollea, dejar el default + `overscroll-behavior:contain`. Testear el scroll táctil en Chromium real con `hasTouch` + gesto por CDP (jsdom no sirve).

## Estado al 2026-07-26

**Tooltip de tonalidad del director se desbordaba del marco en móvil (PR #45, commit `15e014c`).** Reportado con captura (estético): en Nueva/Editar Orden, el tooltip "Esta es la primera vez que el director la va a cantar…" se salía de la pantalla a la derecha en el celular.
- **Causa:** el tooltip (`Ordenes.jsx`, key-history) tenía `whitespace-nowrap` (una sola línea) y estaba centrado (`left-1/2 -translate-x-1/2`) sobre el botón de tonalidad, que vive pegado al borde derecho de la fila → el texto largo desbordaba fuera del viewport.
- **Fix:** anclar a la derecha (`right-0`) + permitir wrap (quitar `whitespace-nowrap`) con `max-w-[13rem] w-max`; el caret pasa a `right-3`. Sólo visual. Verificado en Chromium 390px: antes llegaba a x=445 (fuera de 390), con el fix queda en [131,339] en 3 renglones. Auditado: no hay otros tooltips con este patrón (los de Repertorio son dropdowns `left-0 right-0`).

**Drag & drop de secciones de canción no funcionaba (PR #46, commit `0633ba6`).** Reportado: en Editar Canción, el handle (GripVertical) de las secciones no reordenaba nada (ni celu ni compu).
- **Causa:** el `<GripVertical>` era **puramente decorativo** — el editor de estructura en `Repertorio.jsx` nunca tuvo lógica de DnD conectada.
- **Fix:** se conectó `@dnd-kit` (mismo patrón ya probado en `Ordenes.jsx` para reordenar canciones): `SortableSection` envuelve cada sección, el GripVertical pasa a ser el ÚNICO drag handle (recibe los `listeners`), `PointerSensor` (umbral 6px) + `KeyboardSensor`, `touch-none` en el handle. IDs estables `_localId` por sección (contador `useRef`), **stripeados en `handleSubmit`** (nunca se persisten).
- **Verificación:** Chromium real (Playwright, drag por puntero): arrastrar 'Coro' al tope → [Coro, Intro, Verso 1]; arrastrar desde el input NO reordena.

**Landmines nuevos:**
22. **`song.structure` NO se persiste con `_localId`.** El editor de Repertorio asigna un `_localId` efímero a cada sección para el drag-and-drop; `handleSubmit` lo stripea (`structure.map(({_localId, ...rest}) => rest)`) antes de `addSong/updateSong`. Si agregás campos al editor, mantené ese strip o el `_localId` se filtra a la DB. El DnD de secciones usa el MISMO patrón que las canciones de Ordenes (`@dnd-kit`, handle-only con GripVertical/⋮⋮). Un GripVertical sin `useSortable`+`DndContext` es decorativo y no arrastra — testear el reorden en Chromium real (jsdom no simula el pointer drag).

## Estado al 2026-07-27

**Dos glitches estéticos de iOS Safari en "Nueva Orden" (PR #48, commit `<pendiente>`).** Reportado con captura (iPhone): (a) el campo Fecha se veía mucho más chico que Hora/Banda; (b) el switch "Programar ensayo" se veía deformado/ovalado. Ambos son bugs de **WebKit/iOS** (Chromium no los reproduce), arreglados en `src/index.css` (global → alcance total, beneficia a toda la app):
- **Fecha chica:** iOS Safari colapsa la altura de un `input[type=date]`/`[time]` **vacío** (no hay contenido que sostenga la línea; Hora tenía "20:00" y no colapsaba). Fix: `min-height: 50px` para `input:not([checkbox/radio/range/file/color])` y `select`. **Auditado:** se EXCLUYEN checkbox/radio/range/file/color (si no, el checkbox de `Login.jsx` y los `range` de zoom del recortador se deformarían — verificado en Chromium que quedan en 30/46px, no 50); ningún input/select fija `h-*` chico; los textarea quedan fuera. Bonus: mejora el target táctil.
- **Switch ovalado:** el track es un `<button>` y el reset global de `button` NO tenía `appearance: none`, así que iOS le aplicaba `-webkit-appearance: push-button` (forma nativa) y lo deformaba. Fix: `-webkit-appearance: none; appearance: none;` en `button` (todos los botones ya tienen estilo propio → seguro; arregla cualquier toggle/pill en iOS).
- **Verificación:** WebKit de Playwright no se pudo instalar (el proxy bloquea la descarga con 403), así que el bug iOS no se pudo reproducir con el motor real. Se reprodujo el MECANISMO en Chromium (`appearance:auto` vs `none`; medición de alturas con/sin `min-height`) y se aplicaron los fixes canónicos; no-regresión verificada en Chromium (inputs parejos a ~50px, switch 48×28, checkbox/range intactos). La validación visual final en un iPhone real queda para confirmar, pero son los fixes estándar de estos bugs.

**Seguimiento (PR #49, commit `<pendiente>`):** la Fecha quedó perfecta, pero el switch **seguía ovalado en iOS** aun con `appearance: none` (WebKit igual lo deformaba/comprimía). Fix definitivo en `Ordenes.jsx`: se reemplazó el `<button>` del switch por el patrón **`<label>` + `<input type="checkbox" class="sr-only peer">` + dos `<span>` (track/knob)** con tamaños fijos en px (track `w-[52px] h-8`, knob `h-7 w-7`, `peer-checked:translate-x-5`). Un `<span>` con dimensiones explícitas es **inmune** al `-webkit-appearance` de iOS (no es un control nativo); `shrink-0` + el texto en `flex-1 min-w-0` evitan que el track se comprima. Verificado en Chromium: toggle OFF→ON (gris→verde, knob 2→22px), track constante 52×32, sin comprimirse junto al texto largo.

**Landmine nuevo:**
23. **iOS Safari y los form controls:** (a) un `input[type=date]`/`[time]` **vacío** colapsa su altura en iOS → mantené el `min-height` global (excluyendo checkbox/radio/range/file/color, que se deforman con altura mínima). (b) **Los toggles/switches tipo pill NO deben ser un `<button>`** — iOS Safari les impone forma nativa y los deforma aunque pongas `appearance: none`. Usá el patrón `<label>` + checkbox `sr-only peer` + `<span>` track/knob con tamaños fijos (ver switch "Programar ensayo" en `Ordenes.jsx`). El `-webkit-appearance: none` global en `button` (index.css) igual conviene para los demás botones. Estos bugs NO se ven en Chromium (son de WebKit); validá el toggle (OFF/ON, no-compresión) en Chromium con el patrón exacto.

## Estado al 2026-08-02

**Dashboard/Inicio — card Órdenes contaba de más + cards ahora son accesos directos (PR #50, commit `61ab098`).** Reportado con captura: el card "Órdenes" del inicio contabilizaba TODAS las órdenes (`orders.length`) — con el tiempo serán cientos (pasadas, completadas, canceladas) y el número deja de significar "lo que viene". Además ninguno de los 4 cards era clickeable.
- **Fix 1 (conteo):** el card Órdenes ahora cuenta sólo `status === 'scheduled'` (`upcomingOrders`, que ya existía y usa "Próximos Servicios"). Los otros 3 (Miembros activos, Bandas, Canciones) ya contaban bien.
- **Fix 2 (cards como acceso directo por permiso):** cada card enlaza a su sección **sólo si el rol tiene acceso**, con el MISMO criterio que la navegación (`MobileNav`/`Header`) y los route guards de `App.jsx`: **Miembros** → `/miembros` sólo `['pastor','leader']` (a un `member` le queda **informativo/no-clickeable**, igual que `<MembersOnlyRoles>` lo redirige); **Bandas/Repertorio/Órdenes** abiertos a todos. Se agregó `useCurrentRole()` en `Dashboard.jsx`; `canAccess = !stat.roles || stat.roles.includes(role)`; card accesible se envuelve en `<Link>`, si no queda `<Card>` plano.
- **Verificación:** Chromium real (Playwright) renderizando el `Dashboard.jsx` real con los 3 roles (mock del store + router): con 5 órdenes (2 scheduled, 2 completed, 1 cancelled) el card muestra **2** (control negativo: `orders.length` daba 5); pastor/líder ven los 4 cards como link; miembro ve "Miembros" informativo y el resto como link; 0 errores de consola. lint + 35 tests + build OK.

**Landmine nuevo:**
24. **Cards de estadística del Dashboard = links con permiso.** El conteo de "Órdenes" cuenta SÓLO `scheduled` (no `orders.length`, que crecería sin techo). Cada card es `<Link>` sólo si el rol tiene acceso a la ruta (fuente de verdad: mismo criterio que `App.jsx`/nav — `/miembros` es pastor/líder); si no, `<Card>` plano informativo. Si agregás un card nuevo, definí su `to` + `roles` y respetá el gate `canAccess`, alineado con el route guard correspondiente, o un rol sin acceso clickearía a una ruta que lo redirige.

## Estado al 2026-08-03

**Ensayómetro Fase 1 — práctica personal por orden + glosario "ensamble" (PR #54).** Feature nueva pedida por Paul, primera de 3 fases (F2 pendiente: alarma personal + push diario 18:00 anti-spam; F3: metrónomo BPM + festejo 100% + limpieza automática).

- **Glosario del ministerio (¡importa para todo copy futuro!):** el **"ensamble"** es el encuentro de TODA la banda para ensamblar las canciones (lo que se programa en el orden); el **"ensayo"** es la práctica PERSONAL previa de cada músico. Renombrado en: switch "Programar ensamble" (Nueva/Editar Orden), calendario ("Ensamble · banda"), card del inicio ("¡Hoy tenés ensamble!"), wizard de bienvenida y el push 2 h antes (migración `20260803_ensamble_push_copy.sql`, copy-only: ventana/dedup/REVOKE intactos — el REVOKE se re-asertó porque `CREATE OR REPLACE` resetea grants).
- **Pantalla "Mi Ensayo"** (`/practica/:orderId`, `src/pages/Practica.jsx`, lazy en `App.jsx`; título dinámico en `pageTitles.js`): por canción del orden → contador de **pasadas** ("La practiqué" / botón restar), 3 **checks de dominio** (letra/estructura/frases y arreglos), **dificultad percibida** (toggle Fácil/Media/Difícil), viewer de **acordes en el tono del orden** (usa `transposeSongStructure`) y link YouTube. Arriba, el **Ensayómetro**: anillo SVG de progreso (4 hitos por canción: ≥1 pasada + 3 checks) + mensajes de ánimo + badge con fecha/hora del ensamble. Autoguardado con debounce 500 ms por canción + flush al desmontar; indicador "Guardando…/Guardado". Acceso: card "Practicar este orden" en el detalle del orden (sólo `scheduled`); orden inexistente → redirect a /ordenes (espera al store si aún carga).
- **DB:** tabla `practice_logs` (migración `20260803_practice_logs.sql`) — `UNIQUE (user_id, order_id, song_id)`, `user_id DEFAULT auth.uid()` (el cliente NUNCA lo manda), FK a orders/songs con `ON DELETE CASCADE`, RLS owner-only en los 4 verbos, GRANT explícito a authenticated (regla #7), sin triggers. En el store: `fetchPracticeLogs(orderId)` + `upsertPracticeLog(log)` (upsert `onConflict: 'user_id,order_id,song_id'`), converters con el patrón DATA-LOSS LANDMINE documentado. Deliberadamente FUERA de `initialize()`/realtime/localStorage (dato personal por-orden, sólo lo usa la pantalla).
- **QA:** RLS transaccional en prod 9/9 (BEGIN…ROLLBACK impersonando 2 usuarios reales + anon: owner correcto vía DEFAULT, upsert no duplica, cross-write bloqueado por WITH CHECK, el otro ve/actualiza/borra 0, anon 0, sin residuos). Chromium real 18/18 (viewport 390px + hasTouch, `Practica.jsx` REAL con store mockeado vía plugin `resolveId` de vite: precarga, %, upsert con objeto COMPLETO, restar deshabilita en 0, dificultad toggle, viewer transpuesto C→D y no-transpuesto, redirect, 0 errores de consola). Advisors: 0 alertas nuevas. Lint + 35 tests + build OK.
- **Operativa git:** la rama remota `claude/mobile-photo-upload-3asq58` conserva historia pre-squash de PRs viejos (árbol == main); el force-push está bloqueado en este entorno → rebasar el commit nuevo SOBRE la punta remota (`git rebase --onto origin/<rama> origin/main <rama>`) y pushear fast-forward.

**Landmines nuevos:**
25. **`practice_logs` es owner-only por diseño:** el cliente NUNCA escribe `user_id` (lo pone el DEFAULT `auth.uid()` y RLS lo verifica con WITH CHECK). `upsertPracticeLog` exige el objeto COMPLETO (mismo contrato anti data-loss que los otros converters — `Practica.jsx` siempre guarda logs completos en su estado `logs`). No agregar SELECT de práctica ajena "para el pastor": es herramienta personal, no de auditoría (decisión de producto).
26. **Glosario ensamble vs ensayo:** en TODO copy nuevo, "ensamble" = encuentro de la banda (programado en el orden), "ensayo" = práctica personal (pantalla Mi Ensayo). No volver a llamar "ensayo" al evento de la banda.

## Estado al 2026-08-03 (II)

**Ensayómetro Fase 2 — alarma personal + push diario 18:00 con anti-spam (PR #55).** Segunda de 3 fases (F3 pendiente: metrónomo BPM + festejo 100% + limpieza automática).

- **Alarma de ensayo (opt-in, personal):** toggle en "Mi Ensayo" ("Te recordamos todos los días a las 18:00 mientras tengas canciones por practicar"). Tabla `practice_alarms` (migración `20260803_practice_alarms.sql`): `user_id` PK con DEFAULT `auth.uid()` (el cliente nunca lo manda), RLS owner-only en los 4 verbos, GRANT explícito. Store: `fetchPracticeAlarm()` (sin fila = apagada) + `setPracticeAlarm(enabled)` (upsert `onConflict: 'user_id'`); el toggle es optimista y REVIERTE si el guardado falla. Switch con el patrón iOS-safe (landmine 23).
- **Push diario:** `send_practice_reminders()` (SECURITY DEFINER, search_path fijo, REVOKE del RPC) + cron `practice-reminders` `0 21 * * *` (18:00 ART). Manda UNA notificación `'reminder'` por usuario/día SOLO si: alarma activada + integra la banda de ≥1 orden `scheduled` con fecha ≥ hoy ART y canciones cargadas + su Ensayómetro < 100% en ese orden. El progreso server-side replica los 4 hitos de la pantalla y SOLO cuenta logs de canciones que siguen en el orden. Dedup por día ART matcheando el título `'🎸 Tu ensayo te espera'`.
- **QA:** función 6/6 en prod (`session_replication_role=replica` + ROLLBACK: manda 1, dedup diario, 100% no manda, alarma off no manda, sin opt-in no recibe, sin órdenes no manda, 0 residuos). RLS de `practice_alarms` 9/9 (impersonando 2 usuarios + anon). Chromium real 23/23 (los 18 de F1 + 5 del toggle, incluido el revert en fallo). Advisors 0 nuevas. Lint + 35 tests + build OK. La tabla arranca vacía → el cron no molesta a nadie hasta que alguien active su alarma.

**Landmines nuevos:**
27. **El dedup diario del push de práctica matchea por TÍTULO** (`'🎸 Tu ensayo te espera'` en `send_practice_reminders()`): si cambiás el copy del título, cambiá TAMBIÉN el string del `NOT EXISTS` o el dedup se rompe (doble push el día del deploy). El progreso server-side (4 hitos/canción, solo canciones vigentes del orden) debe mantenerse en sincronía con `milestonesOf` de `Practica.jsx`.
28. **`practice_alarms` es opt-in y sin fila = apagada:** `fetchPracticeAlarm` devuelve `false` si no hay fila; no crear filas por default para todos (sería spam masivo el primer día). El toggle optimista DEBE revertir si `setPracticeAlarm` devuelve null (patrón anti-mentira, testeado en Chromium).

## Estado al 2026-08-03 (III)

**Ensayómetro Fase 3 — metrónomo BPM + festejo 100% + limpieza automática (PR #57).** Cierra las 3 fases del Ensayómetro. Además, PR #56: `.claude/settings.json` con allowlist de permisos de solo-lectura para Claude Code (menos prompts en sesiones web; nada mutante en la lista).

- **Metrónomo por canción** (`Practica.jsx`, hook `useMetronome`): botón con ícono Timer + BPM en cada canción que tenga `bpm`. Web Audio API sin dependencias: beeps agendados por adelantado en el reloj del AudioContext (preciso, no setInterval); acento en el beat 1 del compás (numerador de `song.compass`, default 4); punto visual que late con `animation-duration = 60/bpm`. UN solo metrónomo activo a la vez; al desmontar la pantalla se apaga y cierra el AudioContext.
- **Festejo 100%:** overlay de confeti (44 piezas CSS, keyframes `confetti-fall` en `index.css` con drift por custom property) + card "🏆 ¡Orden dominado!", 4,2 s y se auto-oculta. Dispara SOLO en la transición en vivo `<100 → 100` (ref del percent previo); entrar a una pantalla ya al 100% NO re-festeja.
- **Limpieza automática (DB):** `cleanup_practice_logs()` + cron `practice-cleanup` `30 7 * * *` (04:30 ART): borra `practice_logs` de órdenes NO-`scheduled` con `date < hoy ART - 7` (gracia de 7 días por si un orden se "Reabre"; el borrado de orden ya limpia por CASCADE). Blindada (SECURITY DEFINER + search_path + REVOKE). Migración `20260803_practice_cleanup.sql`.
- **QA:** limpieza 4/4 en prod (replica + ROLLBACK con 3 órdenes sintéticos: borra el completado viejo, conserva el reciente en gracia y el programado, idempotente). Chromium real 35/35 (los 23 de F1+F2 sin regresión + 12 de F3: beeps contados instrumentando AudioContext, un-solo-activo, stop real, festejo en la transición exacta 7/8→8/8, auto-ocultado, no re-festeja tras reload al 100%). Lint + 35 tests + build OK.

**Landmines nuevos:**
29. **Metrónomo:** el AudioContext se crea recién en el primer tap (política de autoplay móvil: el audio DEBE nacer de un gesto del usuario — no crearlo en un useEffect de montaje o iOS lo deja mudo). Los beeps se agendan en el reloj del AudioContext con lookahead, NUNCA sonar directo en el tick del setInterval (drift audible). Para testear audio en Chromium headless: `--autoplay-policy=no-user-gesture-required` + instrumentar `createOscillator` vía `addInitScript`.
30. **Limpieza de práctica con gracia de 7 días:** el cron borra logs de órdenes no-programados con fecha < hoy-7. Si se "Reabre" un orden con más de una semana, la práctica previa de los músicos ya no está (aceptado por diseño: los datos son efímeros). No achicar la gracia sin avisar a Paul. El festejo del 100% dispara sólo en la transición (ref `prevPercentRef`): si tocás el cálculo de `percent`, cuidá no re-disparar en carga.

## Estado al 2026-08-03 (IV)

**"Cerrar" del modal pedía DOS taps en iPhone (PR #58).** Reportado por Paul con captura (Detalle de Orden): el primer tap sobre "Cerrar" no hacía nada, el segundo cerraba.
- **Descartado con repro en Chromium real** (Modal.jsx REAL + BrowserRouter + cableado exacto de Ordenes con `?order=`, ida/vuelta a Practicar, back del navegador): los 6 flujos cierran con UN tap y un tap táctil real también → la lógica historial/pila/counter NO era la causa.
- **Causa (iOS, misma clase que el "Guardar" del recortador de PR #28):** la PWA corre standalone con status bar `black-translucent` → el contenido fluye DEBAJO del status bar. Con contenido alto, el card del modal queda pegado al borde superior y el botón "Cerrar" roza la **zona del status bar / gesto del sistema**, donde iOS se come el primer tap. Además iOS **ignora `user-scalable=no`** desde iOS 10 → el double-tap-to-zoom sigue vivo y puede retener taps sobre controles.
- **Fix doble:** (a) `Modal.jsx`: el overlay suma `env(safe-area-inset-top)` al padding superior (y el card lo descuenta de su `max-height`) → todo el modal queda por debajo de la zona del sistema; sin notch `env()=0`, idéntico a antes. (b) `index.css`: `touch-action: manipulation` en el reset global de `button` → elimina el doble-tap sobre botones (el tap dispara el click al toque) sin afectar scroll/pinch; los drag handles usan la clase `.touch-none` que le gana por especificidad.
- **Auditoría del patrón:** header móvil y menú de MobileNav ya respetaban el inset; sheets son bottom-anchored; el cropper ya estaba arreglado (PR #28). El único expuesto era el `<Modal>` compartido → el fix beneficia a TODOS los modales.
- **Verificación:** Chromium no reproduce el mecanismo iOS (como landmine 23); se validó el repro 6/6 sin regresión, estilos computados correctos (calc con env() parsea: 16px/684px sin notch), QA de Practica 35/35, lint + 35 tests + build. Confirmación visual final en iPhone queda para Paul.

**Landmine nuevo:**
31. **Overlays con contenido alto en iPhone:** cualquier overlay `fixed inset-0` con elementos interactivos cerca del borde superior debe sumar `env(safe-area-inset-top)` a su padding (la PWA es standalone + `black-translucent`: el contenido fluye bajo el status bar y iOS se come los taps en esa zona — síntoma: "el primer tap no anda"). El `<Modal>` compartido ya lo hace; si creás un overlay nuevo, replicalo. Y `button` lleva `touch-action: manipulation` global (index.css): no lo quites, y los drag handles deben seguir usando la clase `.touch-none` (clase > elemento) para conservar su `touch-action: none`.

## Estado al 2026-08-03 (V)

**Eliminar bandas no funcionaba — y canciones y miembros estaban igual (PR #59).** Reportado por Paul: eliminar "Banda Test" confirma, dice "eliminada", pero la banda sigue. Misma clase que el bug de órdenes (PR #42).
- **Causa doble:** (a) `orders.band_id → bands` era `NO ACTION` → borrar una banda con órdenes lanza 23503; (b) el handler de `Bandas.jsx` llamaba `deleteBand()` **fire-and-forget** y mostraba éxito igual. Control negativo transaccional (impersonando pastor ACTIVO): banda, canción con historial y miembro con historial fallaban los tres con 23503.
- **Familia auditada y arreglada** (migración `20260803_fix_delete_fks.sql`): `orders.band_id → SET NULL` (los órdenes históricos permanecen, la UI muestra "Banda eliminada"); `song_key_history.song_id → CASCADE` y `song_key_history.member_id → CASCADE` (la memoria de tono sin esa canción/miembro no referencia nada; 9 de 107 canciones y 2 miembros estaban bloqueados); `pending_registrations.approved_by/rejected_by → SET NULL` (la solicitud histórica se conserva). La EF `admin-delete-member` también chocaba con las FKs (service_role no exime FKs) → ahora funciona.
- **Cliente:** `Bandas.jsx` y `Repertorio.jsx` pasan a `await` + rama éxito/error con `ErrorModal` (patrón PR #42). `Ordenes.jsx` muestra "Banda eliminada" en tarjeta y detalle cuando `band_id` es null (los PDFs ya tenían fallback). `Miembros.jsx` ya esperaba el resultado.
- **QA transaccional en prod (BEGIN…ROLLBACK):** control negativo 3×23503 antes del fix; después: Banda Test se borra y sus 2 órdenes permanecen con `band_id` null, canción y miembro se borran con historial cascadeado, y un rol `member` sigue borrando 0 filas (RLS intacta). Advisors 0 nuevas. Lint + 35 tests + build OK.
- **Trampa de QA descubierta:** hay filas de prueba viejas con `role='pastor'` pero `active=false`; `auth_role()` exige `active=true`, así que impersonarlas hace que TODA la RLS dé false y los DELETE matcheen 0 filas sin error (falso negativo). Al impersonar en QA, SIEMPRE elegir el usuario con `AND active = true`.

**Landmine nuevo:**
32. **Todo handler de borrado DEBE `await` + ramificar** (`deleteBand/Song/Order` devuelven true/false): fire-and-forget + modal de éxito = mentira al usuario cuando la base rechaza (23503 u otro). Las FKs de la app quedaron: `orders.band_id` SET NULL, `song_key_history.{song_id,member_id}` CASCADE, `song_key_history.order_id` SET NULL, `pending_registrations.{approved_by,rejected_by}` SET NULL, `practice_logs.*` CASCADE. Si agregás una FK nueva hacia `bands/songs/members/orders`, decidí la regla de DELETE explícitamente (nunca NO ACTION por defecto) y probá el borrado transaccionalmente impersonando un usuario ACTIVO (ver trampa arriba).

## Estado al 2026-08-06

**Cuatro reportes de Paul con capturas, en dos chunks (PR #60).**

**Chunk A — Bandas:** (a) "Martess"/"Juevess": el card pluralizaba `${label}s` a ciegas; los días terminados en s son invariantes en español. Nuevo `src/lib/days.js` con `dayPluralLabels` (solo sábado/domingo pluralizan) — auditado, era el único lugar con esa concatenación. (b) Las tarjetas de Bandas ahora se ordenan por calendario (semana desde lunes → martes, jueves, sábado, domingo con las bandas reales), desempate por horario y nombre (`compareBandsByCalendar`). 8 tests unitarios con el dataset de las capturas.

**Chunk B — Panel de push (MobileNav móvil + Header desktop, los DOS):** (a) las comunicaciones se pusheaban AL FINAL del array ya ordenado → siempre al fondo aunque fueran lo más nuevo. Ahora cada ítem lleva `createdAt` y todo se mezcla por fecha real (`sortNotificationsByDateDesc` en `src/lib/notifications.js`, con test); los eventos realtime recargan por el mismo loader → camino único. (b) tocar un aviso lo marcaba leído y lo hacía desaparecer (roce accidental = borrado): ahora el tap NO descarta (solo navega en solicitudes), cada card tiene su **✕** (`stopPropagation` → `markAsRead`) y el "Marcar todas como leídas" sigue arriba.

**Landmines nuevos:**
33. **Días de la semana:** NUNCA pluralizar concatenando "s" (`${label}s` → "Martess"). Usar `dayPluralLabels`/`dayLabels` de `src/lib/days.js`. El orden de tarjetas/listas por día usa `compareBandsByCalendar` (lunes primero, domingo último); si agregás una vista nueva ordenada por día, reutilizá esos helpers (tests en `days.test.js`).
34. **Panel de notificaciones:** (a) todo lo que entre al panel DEBE llevar `createdAt` y pasar por `sortNotificationsByDateDesc` — no hay orden implícito por origen; las comunicaciones NO van después. (b) El tap sobre un aviso NO debe marcar leído/descartar (decisión de producto anti-roce): el descarte es SOLO vía la ✕ de cada card o "Marcar todas". El cambio vive duplicado en `MobileNav.jsx` Y `Header.jsx` (los paneles siguen sin unificar): cualquier cambio va en los dos.

## Estado al 2026-09-05

**Avatar mostraba "YUNDEFINED" (PR #77).** Reportado por Paul con captura (ficha de Yessica). Causa raíz: el nombre estaba cargado con **doble espacio**; `getInitials` hacía `name.split(' ')` y `parts[1][0]` sobre el string vacío daba `undefined` → `"Y"+"undefined"`. Fix: `getInitials` movido a `src/lib/initials.js`, robusto a cualquier whitespace (trim + `split(/\s+/)` + filter), **fuente única** de iniciales (el diseño dorado del `<Avatar>` quedó intacto); `normalizeName` en `convertMemberToDB/BandToDB/SongToDB` (trim + colapso) para prevenir; datos limpiados en prod (1 miembro + 98 títulos con espacios sobrantes, whitespace only, migración `20260902_normalize_member_song_names.sql`). Verificado: nada compara por `name`/`title` en el código; 8 tests nuevos; 76 tests + build OK.

**Estudio terminado (sin código): miembros de banda agregados por líderes, permanentes y temporales.** Plan de implementación completo en **`docs/PLAN_membresias_bandas.md`** (decisiones cerradas por Paul, hallazgos verificados, diseño aditivo, migración, QA y orden de PRs). Hallazgo crítico del estudio: la RLS de `bands` permite UPDATE/DELETE a `pastor_or_leader` mientras la UI lo oculta al líder (hueco tipo landmine 19) → el plan lo cierra con política DELETE→pastor + trigger append-only. **Se desarrolla en la sesión local de Paul** siguiendo ese doc.

**Operativa git (aprendido hoy):** la rama de sesión `claude/mobile-photo-upload-3asq58` quedó vieja frente a `main` (main avanzó hasta #76 desde otras sesiones) y el **force-push está bloqueado**; los fixes nuevos salen desde ramas frescas `fix/`/`docs/` creadas de `origin/main` (PR #77 = `fix/avatar-iniciales-espacios`). Con usuarios reales en vivo, **cada PR se mergea solo con el ok explícito de Paul.**

**Landmines nuevos:**
35. **Iniciales/nombres con espacios sucios:** las iniciales salen SOLO de `getInitials` (`src/lib/initials.js`), robusta a dobles/leading/trailing spaces. Los `convertXToDB` normalizan `name`/`title` (`normalizeName`) — la creación de miembros vía EF `admin-create-member` NO pasa por ahí (aceptado: la visualización es a prueba de balas igual). Si agregás una tabla con nombre visible, normalizá en su converter. Nunca volver a `name.split(' ')[i][0]`.
36. **`bands`: la base es MÁS permisiva que la pantalla** (UPDATE/DELETE = `pastor_or_leader`, UI solo pastor). Cualquier trabajo sobre bandas/miembros DEBE empezar por `docs/PLAN_membresias_bandas.md` §2.2 (cerrar el hueco con la regla en la base). No agregar botones "solo pastor" confiando en que la RLS los respalda sin verificarlo con `pg_policies`. **CERRADO por PR A (ver abajo).**

## Estado al 2026-09-05 (II) — miembros de banda por líderes (permanentes/temporales)

Implementada la feature del estudio (`docs/PLAN_membresias_bandas.md`) en **3 PRs**, en el orden §2.8. **PR A y PR B ya están aplicados a producción** (backend, cero impacto para el usuario hasta que el cliente los use); el cliente (PR C) queda a la espera del merge. Todos verificados en vivo; cada PR se mergea solo con el ok de Paul.

- **PR A — Higiene** (`feat/bandas-rls-append-only`, migración `20260905_bands_append_only_leaders.sql`): cierra el hueco #36. Política DELETE de `bands` → `is_pastor()`; trigger `enforce_band_update_rules` (BEFORE UPDATE) que al rol `leader` le exige que el cambio sea SOLO un append a `members` (`NEW.members @> OLD.members`) y ningún otro campo distinto; pastor sin restricciones. QA 10/10 impersonando roles.
- **PR B — Backend** (`feat/bandas-temporales-backend`, migración `20260905_band_temporary_members.sql`): tabla `band_temporary_members` (ventana 1–90 días por CHECK, FORCE RLS, 4 políticas, guard anti-duplicado con advisory lock); helper `band_effective_member_ids(uuid)` = permanentes ∪ temporales vigentes; los 2 crons pasan a usar el helper (cambio quirúrgico + re-REVOKE). QA 14/14 + cron real en replica (temporal recibe, permanente sin regresión) + advisors 0 nuevas.
- **PR C — Cliente** (`feat/bandas-temporales-cliente`): store (carga tolerante + realtime + `getEffectiveBandMemberIds`/`getBandMembers` con `{temporary,expiresAt}` + `addPermanentBandMember`/`addTemporaryBandMember`/`removeTemporaryBandMember`); los 7 consumidores usan miembro efectivo; UI de Bandas (botón "Agregar miembro" líder+pastor, modal permanente/temporal, badge, ✕ del pastor). Lint + build OK; test de paridad; E2E de UI pendiente sobre el preview de Vercel.

**Landmines nuevos:**
37. **Pertenencia de banda = permanentes ∪ temporales vigentes.** `bands.members` (uuid[]) son SOLO permanentes; los temporales viven en `band_temporary_members` (vigente = `expires_at > now()`). NUNCA leer `band.members` directo en lógica de avisos/elegibilidad: usar `public.band_effective_member_ids(bandId)` (SQL, en los crons) o `getEffectiveBandMemberIds(bandId)`/`getBandMembers(bandId)` (cliente). Los 7 consumidores (§1.3 del plan) ya están migrados; **rehacé el grep `\.members\b|getBandMembers|getEffectiveBandMemberIds` antes de tocar pertenencia** por si aparece uno nuevo. El vencimiento es por FILTRADO (nada se borra); un temporal vencido desaparece en silencio.
38. **El candado "líder solo agrega" vive en la base, no en la UI** (trigger `enforce_band_update_rules` + DELETE→pastor). No lo quites ni relajes el superset check. El líder agrega un PERMANENTE con un update DIRIGIDO solo a `members` (`addPermanentBandMember`), **NO `updateBand`**: `convertBandToDB` coerce `meeting_time` null→'20:00' y normaliza el nombre, lo que haría que el trigger vea "un campo cambió" y rechace el append del líder. (Hoy ningún banda tiene esos valores, pero el update dirigido es a prueba de futuro.)
39. **`CREATE OR REPLACE` de `send_rehearsal_reminders`/`send_practice_reminders` resetea grants** → re-asertar SIEMPRE `REVOKE EXECUTE … FROM PUBLIC, anon, authenticated` + `GRANT EXECUTE … TO service_role` (ACL objetivo `{postgres, service_role}`). Mismo patrón que el REVOKE de PR #54. Verificar con `proacl` que no quede `authenticated`/PUBLIC.
40. **`band_temporary_members`:** ventana 1–90 días por CHECK (piso Y techo, fuente de verdad además de la UI). El **cliente DEBE mandar `starts_at` y `expires_at` del mismo instante** (`starts_at`=now cliente, `expires_at`=starts+N días); si manda solo `expires_at` (dejando `starts_at` al default `now()` del server), el skew de reloj puede romper el CHECK. Duplicado-vigente lo frena un trigger BEFORE INSERT con `pg_advisory_xact_lock` por par (airtight bajo doble tap). Tabla con FORCE RLS (como bands/members). `INSERT` = pastor/líder firmando como uno mismo (`added_by` = mi member id); `UPDATE/DELETE` = solo pastor.

## Estado al 2026-09-05 (III) — cierre de huecos de escalada (auditoría adversarial)

Barrido de seguridad destructivo (Workflow de 5 agentes: RLS sweep + SECURITY DEFINER ACL + red-team del diseño, con verificación adversarial de cada hallazgo). Cerró DOS huecos reales explotables por cualquier miembro autenticado, en **una PR** (migraciones ya aplicadas a prod). QA transaccional 15/15 (ataque bloqueado + flujos legítimos intactos) + 4/4 backend sin regresión + advisors 0 nuevas.

- **Escalada de privilegios en `members` (cierra #36 para members):** la RLS `members_update_self_or_pastor` no restringía columnas → un miembro, en su PROPIA ficha, podía `role='pastor'` (escalada directa) o `editor=true` (escritura del repertorio vía `songs_update_editors`). Fix: trigger `enforce_member_update_rules` (BEFORE UPDATE, migración `20260905_members_enforce_update_rules.sql`) que **congela** las columnas privilegiadas (`role, editor, active, user_id, id, pastor_area, leader_of, password_hash, created_at`) para actores autenticados no-pastores; exime backend por `rolbypassrls` (service_role/postgres/crons) y pastores por `is_pastor()`. + `role/editor/active` ahora `NOT NULL`. La RLS decide QUÉ fila; el trigger, QUÉ columnas.
- **Suplantación/spam en `notifications`:** la policy de INSERT (roles {public}, `with_check auth.uid() IS NOT NULL`) no tenía guarda de columnas → un miembro podía POSTear `{is_global:true,...}` y aparecer en el feed de TODOS + disparar un **push web arbitrario a todos los dispositivos** (trigger `notify_push_on_notification_insert` con `to:'all'`). Fix (migración `20260905_notifications_insert_guard.sql`): `REVOKE INSERT ... FROM authenticated, anon` + `DROP POLICY "Allow insert notifications"`. Toda creación legítima es server-side (12 funciones SECURITY DEFINER owner postgres); el cliente nunca inserta en `notifications`.

**Landmines nuevos:**
41. **`enforce_member_update_rules` DEBE ser SECURITY INVOKER** (nunca DEFINER): el candado usa `current_user` para eximir al backend `rolbypassrls`. Con DEFINER correría como el dueño `postgres` (rolbypassrls=true) → la 1ª línea eximiría a TODOS → **no-op silencioso que reabre el hueco sin ningún error**. Columnas congeladas: `role/editor/active/user_id/id/pastor_area/leader_of/password_hash/created_at`. Un self-edit legítimo pasa porque el cliente reenvía valores idénticos (convertMemberFromDB↔ToDB fieles → NEW=OLD). Los EF admin-* que cambian role/editor/active corren como service_role (exentos). **RIESGO LATENTE:** el trigger es UPDATE-only; hoy es suficiente porque `members` NO tiene policy de INSERT/DELETE para authenticated. Si alguna vez se agrega una policy de INSERT (p.ej. revivir auto-registro), DEBE `WITH CHECK` forzar `role='member' AND editor=false AND user_id=auth.uid()` y espejar el freeze en una rama BEFORE INSERT. (El índice `members_user_id_unique` — `UNIQUE(user_id) WHERE user_id IS NOT NULL` — YA está: hace determinista a auth_role() aunque su SELECT use LIMIT 1 sin ORDER BY.)
42. **`notifications`: el cliente NUNCA inserta** (INSERT revocado de authenticated/anon; sin policy de INSERT). Toda notificación nace server-side de 12 funciones SECURITY DEFINER (owner postgres) — triggers de alta de order/band/member/song/pending + crons devocional/reflexión/cumpleaños/ensayo/práctica. Nunca re-otorgar INSERT al cliente; cualquier notificación originada en cliente va por una EF service_role que valide el rol del emisor. El cliente solo SELECT + marcar-leído (en `notifications_read`/`communication_notifications`).

## Estado al 2026-09-05 (IV) — Solicitar colaboración (reemplazo para un servicio)

Feature nueva completa: un pastor/líder pide un reemplazo para un servicio; los elegibles reciben correo+push+banner con "Yo me ofrezco"; el que pide recibe aviso por cada voluntario; al elegir uno y "Cubrir", ese voluntario se suma **temporal** (reusa `band_temporary_members`) y se avisa a aceptado y no-aceptados. Diseño revisado por auditoría adversarial (15 agentes) ANTES de aplicar; QA en vivo: RPC 9/9 (elegibilidad, idempotencia, concurrencia, autorización) + RLS 6/6 (visibilidad/privacidad + escritura de cliente bloqueada) + advisors sin sorpresas.

- **DB:** 2 tablas — `collaboration_requests` (banda+orden+categorías[]+quién pidió+estado open/covered/cancelled/expired) y `collaboration_participants` (una fila por elegible: invited→offered→accepted/declined, UNIQUE(request,member)). **Cliente SOLO lee** (2 policies SELECT); toda escritura es server-side. Migraciones `20260905_collaboration_requests.sql` + `_rpcs.sql` + `_email_templates.sql`.
- **Lógica:** 5 RPCs SECURITY DEFINER blindadas (REVOKE authenticated, GRANT service_role): `collab_create` (calcula elegibles = activos con la categoría, con user_id, que NO estén en la banda efectiva, ≠ el que pide), `collab_offer` (idempotente + a prueba de carrera con UPDATE de estado + ROW_COUNT), `collab_cover` (advisory lock por solicitud + CAS status='open' → un solo ganador; alta temporal; accepted/declined), `collab_cancel`, `collab_expire` (cron `collab-expire` 04:00 ART vence las abiertas de órdenes pasados). Orquesta la **Edge Function `collab`** (service_role, verify_jwt) que autentica al que llama por JWT (identidad NUNCA del body) y hace el fan-out best-effort de push (fila en `notifications` type 'collaboration') + correo (`encolar_email`, 4 plantillas `colaboracion-*`).
- **Cliente:** store (carga tolerante + realtime + `getCollaborationFeed`/`getCollaborationVolunteers` + wrappers `requestCollaboration`/`offer`/`cover`/`cancel`); Dashboard: botón `RequestCollaborationButton` (solo pastor/líder) + banners `CollaborationBanner` (invitado/ofrecido/gestionar/resultado) con wizard de cubrir (reusa la UX de temporal). En `SilentBoundary`.

**Landmines nuevos:**
43. **RLS entre tablas hermanas = recursión 42P17.** Una policy de `collaboration_requests` NO puede hacer `EXISTS(SELECT … FROM collaboration_participants …)` y viceversa: Postgres expande la RLS de la tabla referida en tiempo de reescritura → "infinite recursion detected in policy" en TODA lectura autenticada (banners + realtime muertos, aunque service_role funcione). Se rompe con **helpers SECURITY DEFINER** (`my_member_id()`, `collab_is_participant(uuid)`, `collab_is_requester(uuid)`) que corren como postgres (rolbypassrls) → la subconsulta NO dispara RLS y es opaca al reescritor. Mismo patrón que `is_pastor()`. Verificar SIEMPRE con un SELECT autenticado real que devuelva filas (no 42P17) antes de aplicar. (Lo cazó la auditoría adversarial; casi se despliega roto.)
44. **La banda de la solicitud DEBE ser la del orden.** Los recordatorios de ensamble/práctica se resuelven por `order.band_id`; si la solicitud apunta a otra banda, el temporal cae en una banda distinta y el voluntario nunca recibe los avisos del servicio. La EF `collab` valida `order.band_id = bandId` (y rechaza orden sin banda); el modal filtra los órdenes por la banda elegida. No romper ese acoplamiento.
45. **Escritura de colaboración = EF/RPC only.** El cliente tiene GRANT SELECT y REVOKE explícito de INSERT/UPDATE/DELETE (Supabase por default-ACL le da arwdDxtm a authenticated/anon en tablas nuevas — sin el REVOKE quedaría el GRANT aunque la RLS lo tape). El alta temporal del cubrir la hace `collab_cover` (SECURITY DEFINER, bypassa la RLS de `band_temporary_members` pero el anti-dup trigger + CHECK 1-90 siguen; ambos timestamps del mismo `now()` → landmine #40 intacto).

## Estado al 2026-09-05 (V) — Esquema de reunión + "Iniciar servicio" (accesorio)

Feature nueva **100% accesoria** (tablas y pantallas nuevas; NO toca orders/songs/bands/members — solo lee). Un pastor/líder arma un "run-of-show" por orden (secciones repetibles con observación + tiempo opcional; en "Adoración" asigna canciones del orden); el pastor lo guarda como plantilla, el líder la usa. Cuando el orden tiene esquema, aparece **"Iniciar servicio"**: presentador **pantalla completa, POR DISPOSITIVO** (cada músico avanza en su pantalla, sin sync en vivo) con sección actual + cuenta regresiva + Play/Stop/Siguiente (slide) y, en Adoración, una canción por vez con acordes en el tono del orden + **autoscroll**.

- **DB (`20260905_service_schemas.sql`, aplicada + QA 11/11):** `service_schemas` (1 por orden, UNIQUE order_id → upsert) y `schema_templates`. Cliente escribe directo (sin EF — no hay notificaciones). RLS: schema SELECT = pastor/líder **O** integrante de la banda del orden (helper `am_i_in_order_band` SECURITY DEFINER → llama `band_effective_member_ids`, evita cross-table RLS); escritura schema = pastor/líder; plantilla SELECT = pastor/líder, escritura = **solo pastor**. REVOKE del DML de anon (landmine #45).
- **Constantes (`src/lib/serviceSchema.js`):** `SCHEMA_SECTION_TYPES` (15 tipos: adoracion=isSong, otro=isCustom) + frase por defecto por tipo + `computeSchemaTimeline` (encadena duración/inicio-fin) + `nextSchemaLocalId`.
- **Cliente:** store (carga tolerante + realtime service_schemas + upsert/delete schema + save/delete template). Armador `SchemaBuilderModal` (dnd de secciones tipo canción, SelectMenu, asignación de canciones en Adoración) en el detalle de Órdenes. Presentador `IniciarServicio` (`/servicio/:orderId`).

**Landmines nuevos:**
46. **`/servicio/:orderId` vive FUERA del Layout** (hermano de `/login`, en `App.jsx`) para ser pantalla completa sin barra lateral/header. Por eso trae **su propia guarda** (`if (!user) <Navigate to="/login">`) y, en deep-link/refresh con el store vacío, llama `initialize()` él mismo (el Layout no corrió). Estado del presentador = **por-dispositivo en localStorage** (`adorapp_presenter_pos_<orderId>`, `adorapp_presenter_speed`) — nunca en DB. La RLS ya limita ver el esquema a la banda del orden + pastor/líder, así que un ajeno que entra por URL ve "no hay esquema".
47. **Esquema = accesorio, solo-lectura sobre lo existente.** `service_schemas.sections` es jsonb libre; las secciones cargan `_localId` para el dnd que se STRIPEA antes de persistir (landmine #22). En "Adoración" se referencian canciones por `songId` de `order.songs` (per-order key = `songRef.key`, no `song.key`). Nada de esto escribe orders/songs. Si tocás el presentador, mantené el patrón de reset de la cuenta por SECCIÓN (no por canción) y el autoscroll por rAF (no setInterval).

## Estado al 2026-09-06 — Resumen diario de actividad de líderes/editores (PR #95)

Feature nueva **100% backend** (migración `20260906_notify_pastors_on_leader_activity.sql`, aplicada a prod + QA transaccional + correo verificado en vivo en Gmail). Pedido de Paul: que los pastores se enteren de lo que tocan los líderes. Refinado por Paul a **un resumen DIARIO** (no por-acción → sin spam) e incluyendo **también a los miembros EDITORES**.

- **Qué hace:** cron `leader-activity-digest` (`0 1 * * *` = 22:00 ART) → `send_leader_activity_digest(p_now timestamptz DEFAULT now())`. Resume la **ventana móvil [now-24h, now)** de `audit_events` (el log ya existente): movimientos (insert/update/delete) sobre **songs/orders/bands** hechos por **líderes** (`actor_role='leader'`) o **miembros editores** (`members.editor=true`), **excluyendo pastores y crons/service_role** (`actor_role<>'pastor'` + `actor_member_id IS NOT NULL`). Agrupa por autor+tabla+acción (`_leader_activity_phrase`, pluraliza: "agregó 2 canciones"; n=1 muestra el título). Si hubo ≥1 movimiento, a **cada pastor activo**: 1 notificación `type='activity'` (campanita + push por `push_on_notification_insert`) + 1 correo (plantilla `actividad-lider`, layout dorado, `{{items}}` HTML). **Día sin movimientos = 0 envíos.**
- **Seguridad/robustez:** el cron corre FUERA de la transacción del usuario → **imposible** que rompa una acción en vivo. Envío por pastor en `BEGIN/EXCEPTION`. Funciones `SECURITY DEFINER` + `search_path` fijo + `REVOKE` de authenticated/anon. **Hardening:** `REVOKE INSERT/UPDATE/DELETE ON audit_events FROM authenticated, anon` (la RLS ya lo bloqueaba —sin policy de INSERT— pero se saca el grant redundante). **Escape HTML** de todo dato de usuario (título/nombre/actor) porque `email_templates.cuerpo_html` se renderiza RAW (helper `_html_escape`).
- **Anti-spam en capas:** cadencia diaria (1 envío/día) + el worker `send-emails` ya throttlea 5 min por destinatario. El `type='activity'` se agregó al CHECK `notifications_type_check` (si no, INSERT 23514); la campanita lo renderiza con el fallback (cruz ámbar, igual que 'collaboration').

**Refinamientos (mismo día, PR #98) pedidos por Paul:** (a) el digest ahora es **SOLO correo** (sin campanita/push) y **detalla los nombres de las canciones** por acción y autor, agrupando por autor y combinando con "y": *"Leandro agregó 1 canción (La niña de tus ojos) y editó 2 canciones (Digno y Renuévame)"*. Helpers `_join_names` (lista es-ES con tope 9 + "y N más") + `_activity_subphrase`; se dropeó `_leader_activity_phrase`; dedup por fecha vía `email_throttle` key `leader_digest:<fecha>`. (b) el mail de edición de orden ahora arma **`{{cambios}}`** con el **detalle real** (canciones agregadas/quitadas por diff OLD/NEW `songs` resolviendo títulos, + fecha/hora/banda/tipo): *"Se agregó la canción La Bondad de Dios; se quitó la canción El gozo."* — la campanita también lo lleva. NOTA: el cron real disparó a las 22:00 del 2026-09-06 con la versión previa (count-only + campanita); el refinamiento aplica desde la corrida siguiente.

**Chunk 2 (mismo día, PR #96) — aviso instantáneo al EDITAR un orden.** Pedido de Paul: que editar un orden avise igual que crearlo. Trigger nuevo `notify_on_order_update` (AFTER UPDATE ON orders, `notify_order_update`): cuando una PERSONA (auth.uid() no null → excluye el cron auto-complete y updates server-side) cambia el CONTENIDO de un orden (`songs`/`date`/`time`/`band_id`/`meeting_type` — el status/feedback/rehearsal quedan fuera a propósito), avisa por mail (plantilla `orden-editado`) + campanita/push a la **banda EFECTIVA (permanentes ∪ temporales vigentes, `band_effective_member_ids`) ∪ pastores activos, excluyendo a quien editó**. Corre EN la transacción del usuario → todo en BEGIN/EXCEPTION (un fallo del aviso jamás rompe la edición — verificado rompiendo los emails a propósito). Throttle 90s por orden (`email_throttle` key `order_edit:<id>`) contra re-guardados rápidos. 100% aditivo: NO toca `notify_on_order_insert`. **Observado (no tocado):** el ALTA (`notify_on_order_insert`) hoy encola el mail `nuevo-orden` SOLO a `bands.members` crudo (miembros permanentes) — NO usa `band_effective_member_ids` (pierde temporales) NI incluye pastores explícitamente (solo los alcanza el push global). La EDICIÓN sí hace ambas cosas bien. Alinear el alta quedó ofrecido a Paul.

**Landmines nuevos:**
50. **Fecha de orden (`YYYY-MM-DD`) SIEMPRE se parsea LOCAL para mostrar** (`new Date('${d}T00:00:00')`, helper `parseLocalDate` en Ordenes.jsx): `new Date('2026-09-11')` se interpreta en UTC y en ART (−3) muestra el día ANTERIOR (jueves 10 en vez de viernes 11). Bug reportado por Paul (2026-09-07, PR #100) en `formatDate`; mismo patrón local ya usado en PrepBanner/Practica/repertoireInsights (`parseDate`). El calendario (`OrderCalendar` + `dayKey` de `lib/orders`) NO tiene el bug porque `dayKey` devuelve el string `slice(0,10)` sin reparsear. Al mostrar cualquier `order.date`/`rehearsal_date` con `new Date()`, usar el parseo local. **También (PR #100):** el push de alta de orden dice **"Nuevo orden"** (masculino) y muestra la etiqueta visible del tipo de reunión vía `public._meeting_label(meeting_type)` (espejo de `MEETING_TYPES`), no el slug crudo; y el form de orden NO deja guardar sin canciones (ErrorModal en `handleSubmit`, además del botón).
49. **`notify_on_order_update` corre DENTRO de la transacción del usuario** (a diferencia del digest, que es cron): DEBE quedar todo el cuerpo en BEGIN/EXCEPTION o un fallo del aviso tira abajo la edición del orden. Guard doble: `auth.uid() IS NOT NULL` (excluye cron/service_role) **Y** cambio de contenido real (`songs/date/time/band_id/meeting_type IS DISTINCT FROM OLD`) — NO incluir `updated_at`/`status`/`feedback` en el guard o dispara en no-ops, en completar/cancelar y en el cron auto-complete. Destinatarios via `band_effective_member_ids ∪ role='pastor'`, excluyendo `m.user_id = auth.uid()` (el editor). Para QA impersonar seteando `request.jwt.claims->>'sub'` (auth.uid() lo lee aunque el rol siga siendo postgres/service_role; el UPDATE bypassa RLS y las verificaciones ven todo). El trigger de push a deshabilitar en QA es `push_on_notification_insert`.
48. **El digest de actividad se apoya en `audit_events.actor_role`** (snapshot del rol al momento de la acción, que setea `audit_log_trigger` desde `auth.uid()`): crons/service_role llegan con `actor_role`/`actor_member_id` NULL → excluidos. El filtro "editor" usa `members.editor` ACTUAL (join), no el de la época. El trigger de push en `notifications` se llama **`push_on_notification_insert`** (la FUNCIÓN es `notify_push_on_notification_insert` — no confundir al DISABLE en QA). Para QA de `send_leader_activity_digest`, sembrar `audit_events` en una ventana LIMPIA (p. ej. 2025, antes de que existiera la app) y llamar con `p_now` explícito; envolver en `DO … RAISE EXCEPTION` (rollback) con `push_on_notification_insert` deshabilitado. `email_templates.cuerpo_html` es RAW → SIEMPRE escapar dato de usuario antes de meterlo en `{{items}}`/`{{usuario}}`.

## Estado al 2026-09-10 — El resumen diario ahora dice QUÉ pasó de verdad (PR pendiente, rama `fix/resumen-actividad-preciso`)

**Disparador:** el mail "Movimientos del día" del 09/09 decía *"Melanie Tomaselli creó 1 orden de servicio y editó 5 canciones"*. Paul preguntó qué había pasado. Verificado en `audit_events`: Melanie armó el orden del 12/09 (5 canciones) a las 00:09:04 ART y **un segundo después la app selló `songs.last_used` en esas 5 canciones** (`appStore.addOrder → updateSong({lastUsed})`). Solo cambiaron `last_used`/`updated_at`: tonalidades y acordes intactos; el D#m→Dm de "Yeshua HaMashíaj" fue el transportador DENTRO del orden (asignación), no una edición. En todo el historial Melanie nunca cambió una tonalidad; sus 2 únicas ediciones de contenido fueron sobre canciones que ella misma cargó.

**Corrección de criterio (Paul):** los **líderes SÍ pueden editar canciones por rol** (RLS `songs_update_editors` = `is_pastor_or_leader() OR members.editor`; UI `(isPastor || isLeader || currentMember?.editor)`). El temor de un pastor a una edición de acordes es legítimo → el resumen tiene que distinguirla con claridad, no minimizarla.

**Qué cambió (migración `20260910_leader_digest_precise.sql`, 100% DB, cero cambios de cliente):**
- Nueva `activity_digest_items(p_start, p_end)` (STABLE, SECURITY DEFINER, search_path fijo, REVOKE del RPC): recorre `audit_events` (songs/orders/bands; líderes o editores; no pastores; no crons) y clasifica cada evento por `real_keys` = claves de `changes` **menos `updated_at`, `last_used`, `rehearsal_reminder_sent`**. Un UPDATE sin claves reales se IGNORA (ese era el falso "editó 5 canciones"). Líneas por actor con prioridad: `0 ⚠️ cambió la TONALIDAD de canciones del repertorio (C → D)` · `10 ⚠️ editó acordes o contenido de canciones que ya existían` · `20 completó acordes/contenido de canciones nuevas que cargó` (misma persona la insertó desde `p_start − 7 días`) · `30 cargó canciones nuevas` · `40 corrigió datos de la ficha (título/artista/link de YouTube/categorías/BPM/compás)` · `50 eliminó canciones` · `60 armó órdenes: del DD/MM (N canciones)` · `70 cambió las canciones de órdenes: del DD/MM — agregó …; quitó …; cambió el tono en el orden de T (a → b); cambió el director de T → Nombre` (diff `before/after.songs` por `songId`) · `80 cambió el estado de órdenes (completado/cancelado/reabierto)` · `90 editó datos de órdenes (fecha/hora/banda/tipo/ensamble/comentario)` · `100 eliminó órdenes` · `110–140 bandas` (creó / sumó integrantes / quitó integrantes / **sumó integrantes temporales** / editó datos / eliminó). Los temporales salen de `band_temporary_members` (tabla SIN `audit_log_trigger`) por `added_by`+`created_at` en la ventana, con "hasta el DD/MM".
- `send_leader_activity_digest(p_now)` conserva firma, ventana [now−24h, now), filtro, throttle `leader_digest:<DD/MM/YYYY>` y encolado (`encolar_email('actividad-lider', …, prioridad 5)`); ahora arma `{{items}}` como `• <strong>Actor</strong>` + una línea por categoría (todo pasado por `_html_escape`) y cierra con la nota fija *"Cómo leerlo: armar un orden o cambiar el tono dentro de un orden es asignación (usa el transportador) y NO modifica la canción del repertorio. Solo las líneas marcadas con ⚠️ tocan el repertorio."* `{{cantidad}}` = suma de ítems. Se dropeó `_activity_subphrase` (solo la usaba el digest).
- **QA transaccional en prod (BEGIN … RAISE EXCEPTION → rollback, 0 residuos verificados):** control negativo con la función vieja sobre la ventana real de Melanie → "editó 5 canciones"; con la nueva → solo *"armó órdenes: del 12/09 (5 canciones)"*; 2ª llamada el mismo día no re-encola (throttle). 21 eventos sintéticos en ventana limpia (2027) cubriendo las 17 categorías + 1 temporal: cada una sale en su línea, el UPDATE solo-`last_used` y el solo-`rehearsal_reminder_sent` se ignoran, pastor y miembro sin `editor` excluidos, evento fuera de ventana excluido, `<x>` escapado como `&lt;x&gt;`, `error_log` limpio, grants revocados para anon/authenticated, `_activity_subphrase` ausente.

**Landmines nuevos:**
51. **El digest clasifica por CAMPOS reales, no por tabla+acción.** `songs.last_used` lo sella la app al armar un orden (`appStore.addOrder`), `orders.rehearsal_reminder_sent` lo sella el cron y `updated_at` cambia siempre: los tres están en la lista de exclusión de `activity_digest_items` y un UPDATE que solo toque esos NO es actividad. Si agregás una columna "automática" nueva a songs/orders/bands (sellos, contadores, flags de cron), sumala a esa lista o el resumen volverá a acusar ediciones fantasma. Si agregás una columna editable por el usuario, mapeala a su etiqueta legible en el `CASE` de "ficha"/"datos" (si no, un cambio SOLO en esa columna no se reporta). **Asignar/transportar dentro de un orden ≠ editar la canción**: el tono del orden vive en `orders.songs[].key` y se reporta como "cambió el tono en el orden"; la tonalidad del repertorio (`songs.key/original_key`) es la única que lleva ⚠️. `band_temporary_members` NO está auditada → el digest la lee directo; si algún día se le agrega `audit_log_trigger`, quitar esa lectura para no duplicar. Ojo: `orders.songs` persiste basura `_localId/_pendingHistory/_suggestedDirector` del cliente (el diff del digest la ignora porque compara por `songId/key/directorId`; queda como deuda de `Ordenes.jsx`).

## Estado al 2026-09-11 — Estudio (sin código): Formación del orden

Pedido de Paul: al armar un orden, un asistente para definir la **formación** (quiénes tocan ese servicio y con qué instrumento; "Participan todos" con un toque o selector), que se informe en el mail del orden y que **solo** los participantes reciban los avisos de práctica (ensamble 2 h antes, alarma de ensayo 18:00), mientras la info general sigue llegando a toda la banda. Plan completo, hallazgos verificados en base y código, diseño (columna `orders.lineup jsonb` + helper `order_participant_ids`), UX premium, seguridad adversarial, QA y orden de PRs en **`docs/PLAN_formacion_orden.md`**. Decisiones abiertas para Paul en su §1 (la clave: el asistente debe correr ANTES del INSERT, porque el mail `nuevo-orden` se dispara en el trigger de INSERT). Hallazgos colaterales anotados en su §2.5 (mail `nuevo-orden` sin temporales ni pastores; card "hoy tenés ensamble" sin filtro de banda; detalle del orden sin ensamble; `cloneOrder` hereda el ensamble; `<select>` nativo de director). **IMPLEMENTADO el mismo día — ver la sección siguiente.**

## Estado al 2026-09-11 (II) — Formación del orden IMPLEMENTADA (PRs #104 base, #105 cliente)

Feature completa en producción, en 3 PRs siguiendo `docs/PLAN_formacion_orden.md` (§7). Decisiones de Paul: asistente **al tocar Guardar, antes del INSERT** (así el mail `nuevo-orden` sale con la formación); rotación asistida SÍ; directores de canción incluidos y bloqueados; aviso puntual solo a quien entra/sale/cambia; Practicar visible para toda la banda con nota para los que no están en la formación; formación ≠ membresía (nadie sale de la banda); arreglar todos los colaterales.

**Base (`supabase/migrations/20260911_order_lineup.sql`, aplicada a prod):**
- `orders.lineup jsonb` nullable: `NULL` = sin formación (toda la banda efectiva, comportamiento previo); `{mode:'all',members:[]}` = todos (dinámico); `{mode:'custom',members:[{memberId,instruments[]}],definedBy,definedAt}` = lista cerrada.
- Trigger **BEFORE INSERT/UPDATE `validate_order_lineup`** (SECURITY DEFINER): normaliza y valida (shape, modo, uuids, sin duplicados; miembros ajenos a la banda efectiva → rechaza en edición directa / filtra en silencio si cambió la banda; instrumentos restringidos a la ficha del miembro; **directores de canciones siempre incluidos**; lista vacía → `all`; sella `definedBy/definedAt`).
- Helpers blindados (REVOKE de anon/authenticated): `_lineup_participants`, **`order_participant_ids(order_id)`** (fuente de verdad de participantes: custom → lista; all/NULL → `band_effective_member_ids`), `_lineup_member_instruments`, `_lineup_html` (bloque HTML escapado + línea personal "Vos participás como Batería" / "Esta vez no estás en la formación"), `_instrument_rank`, `_safe_uuid`.
- `notify_on_order_insert`: destinatarios ahora **banda efectiva ∪ pastores** (arreglado: antes solo `bands.members` crudo) + `{{formacion}}` en el mail. `notify_on_order_update`: rama de contenido (igual + `{{formacion}}`) y **rama solo-formación** (`added/removed/changed` → mail `formacion-cambio` + campanita `type='order'` SOLO a quien entra, sale o cambia de instrumento; editor excluido; sin throttle). Crons `send_rehearsal_reminders` y `send_practice_reminders` usan `order_participant_ids`. El digest clasifica "actualizó la formación" (prio 65). Plantillas `nuevo-orden`/`orden-editado` reciben `{{formacion}}` por reemplazo quirúrgico (se cerraron `<em>/<strong>` sin cerrar que había en las vivas); plantilla nueva `formacion-cambio`.
- QA transaccional en prod (BEGIN … RAISE → rollback): validador (shapes hostiles, miembro ajeno, director forzado, cambio de banda), participantes por modo, crons solo a participantes, aviso puntual (entra/sale/cambia, editor excluido), mail con bloque, advisors sin alertas nuevas. **Bug preexistente cazado de paso:** `text[] || 'literal'` en `notify_on_order_update` fallaba con "malformed array literal" (3 avisos de edición perdidos el 10/09) → `::text`.

**Cliente (`feat/formacion-orden-cliente`):**
- `src/lib/lineup.js` (puro, espejo de la semántica SQL; tests `lineup.test.js`): `INSTRUMENT_ORDER`, `participantIdsOf`, `isOrderParticipant`, `lineupInstrumentsFor`, `groupByInstrument`, `coverageGaps`, `suggestRotation` (ventana 120 días, solo órdenes custom de la misma banda anteriores a la fecha; sugiere por instrumento a quien hace más tiempo no lo toca, con última fecha y quién lo tocó), `buildLineup`, `lineupSummaryText`.
- `appStore.js`: `lineup` en `convertOrderFromDB/ToDB`; **`stripSongRefs`** quita `_localId/_pendingHistory/_suggestedDirector` antes de persistir `songs` (cierra la deuda de landmine #51); `cloneOrder` no hereda ensamble ni formación; helpers **`getOrderParticipantIds/isOrderParticipant/getOrderParticipants/getOrderLineupGroups`** (tests `appStore.lineup.test.js`).
- `components/orders/LineupEditor.jsx` (dos tarjetas de modo, picker con Todos/Ninguno, buscador >6, chips de instrumento por persona, directores con candado "Dirige N canciones", alerta de cobertura, sugerencia de rotación + "Aplicar sugerencia" — los directores conservan sus instrumentos —, badge temporal), `LineupSummary.jsx` (agrupado por instrumento, "También participan"), `LineupModal.jsx` (edición desde el detalle → `updateOrder({lineup})`).
- `Ordenes.jsx`: paso `formStep='lineup'` al crear (footer Volver/Guardar orden; borrador `lineupDraft`), confirmación "El orden todavía no se guardó" al cerrar; detalle con bloques **Ensamble** (faltaba) y **Formación** + "Editar formación" (pastor/líder); pill en tarjeta; PDF con la formación; selectores Banda/Director/Tono con `SelectMenu` (sin `<select>` nativo); tooltip de historial de tono por fila, auto-cierre 6 s, `pointer-events-none` (interceptaba taps sobre el control de director en móvil), fecha con `parseLocalDate`.
- `Dashboard.jsx`/`PrepBanner.jsx`: card "hoy tenés ensamble" y banner de preparación solo para participantes (pastor siempre); chip "Tocás: X" / "Participás" / "No estás en la formación" en próximos servicios. `Practica.jsx`: nota "No estás en la formación, pero siempre viene bien repasar" (integrante de la banda fuera de la formación). `IniciarServicio.jsx`: línea de formación.
- QA: Chromium real 390px con páginas reales + store real sobre mock de Supabase (arnés en `scratchpad/qa/`, ignorado por git): **49/49**. Lint 0 errores, 98 tests, build OK.

**Landmines nuevos:**
52. **Columna nueva en `orders` = tres lugares.** `convertOrderFromDB` **y** `convertOrderToDB` (si falta en `ToDB`, cualquier `updateOrder` la pisa con NULL — regla #8) **y** la lista de exclusión/etiquetas de `activity_digest_items` (landmine #51). `lineup` ya está en los tres. `orders.songs` se persiste vía `stripSongRefs`: si agregás claves de UI a las canciones del editor (`_algo`), sumalas al strip.
53. **Avisos del SERVICIO = participantes; info GENERAL = banda efectiva.** Ensamble 2 h antes, alarma de ensayo 18:00, card "hoy tenés ensamble", banner de preparación y chips personales usan `order_participant_ids` (SQL) / `isOrderParticipant`·`getOrderParticipantIds` (cliente). El mail/push de alta y edición del orden, el orden en sí, las canciones, Practicar y el esquema siguen usando `band_effective_member_ids`/`getEffectiveBandMemberIds` (landmine #37). No cruzar: un aviso de servicio con banda efectiva vuelve a molestar a quien no toca; info general filtrada por formación deja a ciegas al resto. `lineup` NULL/`all` = banda efectiva dinámica (si alguien se suma después queda incluido); `custom` = lista cerrada.
54. **`validate_order_lineup` debe seguir SECURITY DEFINER y BEFORE.** Lee `members`/`bands`/`band_temporary_members` con RLS de por medio; como INVOKER un líder vería menos filas y el validador filtraría participantes legítimos en silencio. Los directores de canción SIEMPRE quedan dentro (base + `withDirectors` en el editor): si necesitás sacar a alguien, primero cambiá el director. Un cambio de banda re-filtra la formación sin error (miembros ajenos caen; si queda vacía pasa a `all`).
55. **Cambio SOLO de formación NO pasa por la rama de contenido** de `notify_on_order_update`: no hay throttle de 90 s ni mail `orden-editado`; va la rama `formacion-cambio` a los afectados (quien entra/sale/cambia), excluyendo al editor. Si agregás campos al guard de contenido, no incluyas `lineup` o cada edición de formación mandaría el mail general a toda la banda. `updateOrder` desde el cliente SIEMPRE reenvía `lineup` del snapshot (merge), así una edición de canciones no borra la formación.
56. **`text[] || 'literal'` en plpgsql necesita `::text`.** `v_parts := v_parts || 'cambió la banda'` intenta parsear el literal como array → "malformed array literal" y el aviso muere en el `EXCEPTION` (silencioso salvo `error_log`). Castear siempre (`|| 'texto'::text`). Revisar `error_log` después de desplegar cualquier trigger de aviso.
57. **QA de UI con el store real: arnés `scratchpad/qa/`** (Vite con plugin `resolveId` que reemplaza `lib/supabase` por un mock que registra `window.__qa.calls`, fixtures de Banda Sábado + órdenes anteriores para la rotación, `run.mjs` con playwright-core 390px/hasTouch). `scratchpad/` está en `.gitignore`; el mock debe distinguir `single` (PGRST116 sin filas) de `maybeSingle` (`data:null` sin error) o aparecen errores de consola falsos. Los `SelectMenu` se operan tocando el botón (`aria-haspopup`) y luego la opción por texto; los directores bloqueados llevan `aria-disabled` → `click({force:true})` para probar que NO cambian.

## Estado al 2026-09-11 (III) — Multi-área: observadores Multimedia/Sonido + área universal

Feature nueva (plan `docs/PLAN_multi_area.md`). **Backend EN PROD** (migración `20260911_multi_area_observers.sql`, aplicada + QA transaccional 24/24 + auditoría adversarial 20 agentes 0 crítico + advisors 0 nuevos). Cliente (PR2) y EF (PR3) pendientes. Idea: usuarios que NO son de banda (áreas **Multimedia** y **Sonido**), solo-lectura de todo, exportan lo exportable, NO editan/eliminan nada, reciben por **correo** todos los órdenes y las formaciones, y pueden abrir el presentador. Los músicos/líderes actuales quedan etiquetados **Adoración**; pastores = **multiárea por rol** (sin dato). Estructura lista para más áreas (intercesión/danza…), cada una con tratamiento propio. Devocionales/reflexiones ya son push global → los observadores (miembros) ya los reciben, sin código.

- **UNA columna nueva:** `members.areas text[]` (espeja `instruments`; `'{}'` = como hoy). Backfill: `role IN (leader,member) AND active` → `{'adoracion'}` (28); pastores quedan `'{}'`.
- **Registro de capacidades = FUENTE ÚNICA en helpers SQL** (editar el literal para sumar áreas): `_area_email_slugs()`={multimedia,sonido} (reciben mail de todo orden), `_area_formation_slugs()`={sonido} (aviso de cambio solo-formación), `_area_presenter_slugs()`={multimedia,sonido} (abren el presentador). `src/lib/areas.js` (PR2) debe ESPEJAR estos sets + labels.
- **Freeze:** `enforce_member_update_rules` (sigue SECURITY INVOKER, landmine #41) congela **`areas`** además de las otras columnas privilegiadas → **solo un pastor (o backend service_role) cambia el área**. Un miembro que se auto-asigne 'multimedia' → 42501. La columna `areas` DEBE estar en **ambos** converters (`convertMemberFromDB`/`ToDB`) round-trip fiel, o el self-edit rompe el freeze (regla #8).
- **Presentador (única relajación de LECTURA):** `ss_select` ahora `is_pastor_or_leader() OR am_i_in_order_band(order_id) OR can_open_service_presenter()`. `can_open_service_presenter()` es **SECURITY INVOKER** (lee la propia ficha; members SELECT=true) → sin advisor nuevo; REVOKE de anon, GRANT authenticated (necesario para la policy). Solo SELECT; escritura de esquema sigue pastor/líder.
- **Correos:** `notify_on_order_insert` y la rama de contenido de `notify_on_order_update` suman observadores (`m.areas && _area_email_slugs()`) SIN duplicar (DISTINCT). **Observadores = SOLO correo** (la campanita de edición se gatea con `v_is_core` = banda o pastor). En el ALTA la campanita 'Nuevo orden' es global (la ve todo miembro, incluidos observadores — comportamiento preexistente, no spam nuevo). Variables nuevas en las plantillas `nuevo-orden`/`orden-editado`: `{{ensamble}}` (línea de ensamble para TODOS) y `{{area_nota}}` (framing por-destinatario para el observador no-banda). `notify_on_order_insert` ahora envuelto en BEGIN/EXCEPTION (un fallo del aviso jamás tumba el alta).
- **Cambio solo-formación:** además de los participantes, se avisa por **mail (sin campanita)** a los observadores de **Sonido** que NO recibieron el aviso personal (`NOT (m.id = ANY(v_notified))` — `v_notified` acumula a entra/sale/cambia). Así un músico-de-banda que también es Sonido, si su parte no cambió, igual recibe el aviso.

**Landmines nuevos:**
## Estado al 2026-09-12 — Banners de área (Multimedia/Sonido) + avisos a ambas áreas

Banners premium en el inicio para los observadores (`src/components/dashboard/ObserverAreaBanners.jsx`, lógica pura en `src/lib/observerBanners.js`): identidad (logo del área), orientación (servicio hoy / ensamble hoy [solo Sonido] / orden próximo / **¡Ojo! Hubo cambios**) y atajos. **Multimedia**: "Ver el orden" + "Descargar letras (Word)" (export `.docx` de letras por sección SIN acordes, `src/lib/lyricsDocx.js`, lazy-import de `docx`). **Sonido**: "Ver el orden" + "Abrir presentador" (si hay esquema). Van en el Dashboard DESPUÉS de todo lo de Adoración (prioridad). **Logos**: `public/area-multimedia.png` y `public/area-sonido.png` (blancos/color sobre transparente) van TAL CUAL sobre medallón negro — NO invertir ni filtrar (el error inicial fue invertir el de Multimedia y volverlo invisible). Backend (migración `20260912_area_avisos_ambas_y_cambios.sql`, aplicada + QA 10/10): `orders.content_changed_at` (sello del trigger BEFORE `set_order_content_changed` cuando cambia contenido/formación — NUNCA lo escribe el cliente, no va en convertOrderToDB); `_area_formation_slugs()` → {multimedia,sonido}; `notify_on_order_update` ahora da PUSH además de mail a los observadores en cambios de contenido, y avisa por push+mail a AMBAS áreas en cambio de formación. `content_changed_at` es inocuo en el digest (ninguna rama actúa sobre esa clave; nunca aparece sola).

**Landmines nuevos:**
59. **Logos de área van SIN filtro.** Son blancos/color sobre transparente → sobre el medallón negro se ven "blanco sólido sobre negro". NO aplicar `invert`/dilatación/watermark (invertir el de Multimedia lo vuelve invisible). El banner se auto-oculta si el miembro no es de área observadora o no hay órdenes. `content_changed_at` lo setea SOLO la base (trigger BEFORE) al cambiar contenido/formación → va en `convertOrderFromDB` pero NUNCA en `convertOrderToDB` (mismo patrón que `rehearsal_reminder_sent`, landmine #9); es "automática" pero inocua en el digest. El estado "changed" del banner usa una ventana de 7 días (`changedSinceART`).

58. **`members.areas` = solo-lectura para el propio miembro** (freeze en `enforce_member_update_rules`; solo pastor/EF lo cambian). DEBE estar en ambos converters (round-trip fiel). **`pastor_area` ≠ área de ministerio** (es el pastor que cubre a la persona, texto libre) — no confundir ni tocar. **Adoración es tag informativo:** NO está en `_area_email_slugs`/`_presenter_slugs`, así que etiquetar 28 personas NO cambió a quién le llega ningún correo ni acceso. Las capacidades por área viven en **un** helper SQL (`_area_*_slugs`) + su espejo `src/lib/areas.js`: para sumar un área, editar el literal y decidir sus capacidades explícitamente. **PR3 (EF admin-create/update-member) DEBE poner `setIf('areas', ...)` DENTRO del bloque `if (isPastor)`** (nunca junto a `instruments`/`name`/`phone`, que son self-editables) — es el único vector de auto-asignación que sobrevive al freeze de la base.

## Estado al 2026-09-11 (III) — Formulario "Quiero registrarme" + bordes de botones (bug global)

Pedido de Paul (capturas del iPhone): en "Solicitar registro" el campo "Líder de" decía "Grupo o área que liderás" (debe pedir el **nombre de tu líder del Equipo de Avivamiento**); bajo "Área de ministerio" falta una nota chica en cursiva "(seleccioná el área a la que pertenecés; puede ser más de una)"; y las tres áreas tienen que verse como botones con un reborde dorado delicado y vértices redondeados.
- **Causa raíz del "no se ven como botones" (bug global desde PR #27, jun-2026):** `src/index.css` tenía `button { border: none; }`. Ese `border: none` fija `border-style: none` con especificidad de elemento y le gana al preflight de Tailwind (`* { border-style: solid }`), así que **todo `<button>` con clases `border`/`border-2` renderizaba SIN borde** (verificado en Chromium: 0 de 3 botones con clase `border` en el modal de Órdenes tenían borde). Afectaba a ~150 botones (toggles de área/instrumento, `Button` secundario, `SelectMenu`, filas del editor de formación, etc.). Fix: quitar `border: none` (el preflight ya deja `border-width: 0`; el borde aparece solo donde un componente lo pide). Efecto visible: los bordes diseñados desde junio ahora se ven (p. ej. "Exportar PDF / Imprimir / Editar" del detalle del orden, "Cancelar" de los formularios).
- Registro: placeholder "Nombre de tu líder" + nota "(tu líder del Equipo de Avivamiento)" (el texto completo no entra en una línea a 390 px); nota bajo "Área de ministerio"; áreas e instrumentos con `rounded-xl border border-gold-500/40` (seleccionado: `border-gold-400 bg-gold-500/15`). El mismo campo `leader_of` en perfil móvil/escritorio y en la ficha de Miembros pasó a "Nombre de tu líder". El label pasó de "Líder de" a **"Tu líder"** (Paul lo aprobó, PR siguiente) en Login, ficha de Miembros (form + columna CSV "Líder"; la tabla ya decía "Líder"), Header y MobileNav. Semántica definitiva de `members.leader_of`: **el nombre del líder de la persona** (Equipo de Avivamiento), no lo que la persona lidera.
- QA: Chromium 390 px sobre `Login.jsx` real (arnés `scratchpad/qa/login.html`): placeholder, nota, borde computado 1 px dorado, selección múltiple (Adoración + Multimedia) y aparición de instrumentos; arnés de formación 49/49 sin regresión; lint 0 errores, 98 tests, build OK.

**Landmine nuevo:**
59. **NUNCA `border: none` (ni `border: 0`) en un reset global de `button`/`input`.** Anula el `border-style` del preflight y deja invisibles TODAS las utilidades `border-*` de Tailwind sobre ese elemento, sin ningún error. El reset de `button` en `index.css` deja `background/appearance/touch-action` pero NO toca `border`. Si un botón tiene que ir sin borde, que no lleve clase `border`. Para verificar un borde "que no se ve", medir `getComputedStyle(el).borderTopStyle`/`borderTopWidth` en Chromium: si el style es `none` con una clase `border` presente, hay un reset de elemento pisándolo.

## Estado al 2026-09-12 — Arranque lento: diagnóstico + pantalla "Actualizando a la nueva versión"

**Reporte de Paul:** "Cargando AdorAPP…" demora mucho (captura a las 22:59 tras 6 publicaciones en el día). **Diagnóstico (sin tocar datos):** Supabase sano, payload de arranque chico (~370 KB; songs 335 KB), latencia normal. La demora es del arranque: (1) el SW hace `skipWaiting` al instalarse y `registerSW` recargaba la página en `controllerchange` → **cada primera apertura después de un deploy cargaba DOS veces** (versión vieja → SW nuevo → reload → todo de nuevo, incluido el bundle de ~600 KB); (2) `App.jsx` bloquea toda la UI hasta completar, en serie, `initializeAuth` (getSession + fetchProfile) y `initializeApp` (9 tablas en `Promise.all`), sin usar la copia local de localStorage para pintar antes; (3) tras cada deploy el código se rebaja entero (hashes nuevos). Paul eligió NO cambiar el arranque por ahora sino **explicar la doble carga**: pantalla "Actualizando a la nueva versión…" con barra dorada por hitos reales. El arranque paralelo/stale-while-revalidate queda como mejora compatible para después.

**Implementado (`feat/splash-actualizacion`):**
- `src/lib/updateProgress.js`: estado compartido `{active, step}` con pasos `found → downloading → installed → activating → reloading` (antes de recargar) y `booting → session → data → done` (después). `markUpdateStep` nunca retrocede; `reloading` se persiste en `sessionStorage['adorapp:update']` (TTL 90 s) y **se consume al cargar el módulo** (una segunda carga fallida no repite la pantalla). `finishUpdate` limpia.
- `src/lib/registerSW.js`: `isUpdate = !!navigator.serviceWorker.controller` al registrar. Con update: `updatefound` → found/downloading, `statechange` installed/activating, `controllerchange` → `reloading` + reload a los 250 ms. **Primera instalación (sin controller): sin recarga** (antes recargaba también la primera visita). `onWaiting`/`applyUpdate`/`UpdateBanner` quedan por si algún día se quita el skipWaiting.
- `PageLoader` con `progress`/`caption`/`overlay`: logo + "Actualizando a la nueva versión…" (gold-200) + barra 3 px `.update-bar` (gradiente dorado, glow, shimmer; `prefers-reduced-motion` la apaga) + paso en curso + "versión del DD/MM" (`import.meta.env.VITE_BUILD_DATE`, definido en `vite.config.cjs` con fecha ART del build). `data-testid` `update-loader`/`update-bar`/`update-caption`. El arranque normal NO cambia.
- `App.jsx`: `useUpdateProgress`; en la segunda carga marca `session` tras auth, `data` tras el store, `done` ("Listo" / "Ya tenés la última versión") 650 ms y entra. Si la versión nueva se detecta con la app ya en pantalla, la cubre un `PageLoader overlay` hasta la recarga.
- **QA real con service worker** (`scratchpad/qa/update-flow.mjs`): build con `VITE_SUPABASE_URL/ANON_KEY` (públicas) + `vite preview` en 4173 + Chromium 390 px con `page.route('**/*.supabase.co/**', abort)` (acá el proxy cuelga Supabase); "versión nueva" = `fs.appendFileSync('dist/sw.js', comentario)`. **15/15**: primera visita sin recarga ni pantalla; reapertura con versión nueva → pantalla ANTES de la recarga, recarga sola, pantalla DESPUÉS con pasos reales (Descargando → Reiniciando → Validando tu sesión → Cargando tus datos → Listo), entra al login, marca limpia; reapertura sin novedades → arranque normal. Lint 0 errores, 98 tests, build OK.

**Landmine nuevo:**
60. **Actualizaciones: dos disparadores de la pantalla "Actualizando…".** (a) Modo `build` (el caso real de cada deploy): `VITE_BUILD_ID` vs `localStorage['adorapp:build']`; si tocás `vite.config.cjs`, mantené el `define` de `VITE_BUILD_ID` o la pantalla deja de dispararse en silencio (en dev es `local-<ts>` y cambia en cada arranque: normal). (b) Modo `sw`: doble carga por diseño del SW. `sw.js` hace `skipWaiting()` en `install` y `registerSW` recarga en `controllerchange`: la primera apertura tras cada deploy carga dos veces. La pantalla "Actualizando…" lo explica, no lo evita. Si algún día se quiere evitar, la opción es no recargar (la navegación es network-first: la versión nueva entra sola en la próxima apertura) — hay que decidirlo con Paul. Reglas: `updateProgress` solo se activa con `isUpdate` (había controller); la marca en `sessionStorage` se consume al arrancar (nunca dejarla viva o un arranque roto la repite); los pasos son hechos reales (no meter temporizadores falsos); `VITE_BUILD_DATE` se define en `vite.config.cjs` (ART) — en dev queda vacío y la línea no se muestra. Para probar el SW hace falta un build servido por http (`vite preview`), NUNCA el dev server (registerSW se apaga en DEV); Supabase se corta con `page.route(...abort)` para que el arranque no cuelgue en el sandbox. En el sandbox, sin red, el boot tarda ~20 s por reintentos internos de supabase-js: es del entorno, no de la app.

**CORRECCIÓN (2026-09-12, misma noche):** el diagnóstico (1) era **incorrecto para el caso de Paul**. El browser solo dispara `updatefound` cuando cambian los bytes de `public/sw.js`, y ese archivo **no cambia desde el 18-jun (PR #27)**: ninguno de los 6 deploys del 11/09 provocó recarga. La demora real es (2)+(3): arranque en serie (sesión → ficha → 9 tablas) sin pintar nada + re-descarga del bundle tras cada deploy + arranque en frío del iPhone. La pantalla "Actualizando…" **queda dormida** (solo aparece si algún día cambia `sw.js`); funciona y está probada, pero no cubre el caso reportado. Error de método: afirmar el mecanismo sin verificar el historial del archivo. **Resuelto con OK de Paul (PR `feat/splash-por-version`):** segundo disparador en `updateProgress.js`, modo `'build'`: `vite.config.cjs` define `VITE_BUILD_ID` (= `VERCEL_GIT_COMMIT_SHA[0:12]` en Vercel, `local-<ts>` en local); al cargar el módulo se compara con `localStorage['adorapp:build']` — si hay uno guardado y difiere, esta carga es la primera apertura tras el deploy → pantalla "Actualizando…" con escala `BUILD_PCT` (booting 22 → session 58 → data 86 → done 100); el id actual se guarda de inmediato (una carga fallida no la repite; sin id guardado = primera vez en el teléfono = sin pantalla). El modo `'sw'` (cambio de `sw.js`) sigue existiendo y tiene prioridad. `pctOf(step, mode)` da el % según el modo. QA `scratchpad/qa/build-flow.mjs` 8/8 (sin id guardado → normal; id viejo → pantalla con sesión → datos → Listo y entra; reapertura → normal) + `update-flow.mjs` 15/15 sin regresión.

**DECISIÓN DE PRODUCTO (Paul, 2026-09-12):** se rechaza el arranque "stale-while-revalidate" (mostrar el Inicio con la copia local y refrescar por detrás). Cita: *"quiero que cuando entre al home le aparezca todo lo que le tiene que aparecer en ese segundo, no dos segundos después… Prefiero que demore 4 segundos, pero que al entrar, vea todo lo que tiene que ver."* El arranque queda como está.

**Landmine nuevo:**
61. **El Inicio NUNCA se pinta con datos de la copia local antes de tener los frescos.** Decisión de Paul: la pantalla negra "Cargando AdorAPP…" espera a sesión + ficha + tablas y recién entonces muestra la app; localStorage es SOLO fallback cuando Supabase falla (como hoy en `initialize()`). No implementar stale-while-revalidate ni "optimistic home". Lo único aceptable para acortar la espera es pedir en PARALELO lo que hoy va en serie, siempre esperando a que llegue TODO antes de mostrar. Y no afirmar que "cada deploy recarga dos veces": solo pasa si cambia `public/sw.js`.

## Estado al 2026-09-12 (II) — Arranque en paralelo (ficha + tablas a la vez), sin datos viejos

Con OK de Paul (tras explicarle riesgo y motivo del orden en serie): el arranque conserva la regla de landmine #61 (la app se pinta recién con TODO fresco) pero deja de encadenar viajes al servidor.
- **Antes:** `App.init` → `authStore.initialize()` (getSession local → **ficha**, 1 viaje) → `appStore.initialize()` (**9 tablas**, 1 viaje) → Inicio. Y al montar `RouteSync` (throttle con `lastRefreshAt = 0`) volvía a disparar `initializeApp()` + `refreshProfile()` (que a su vez llama `initialize()` otra vez): **las 9 tablas se pedían 3 veces y la ficha 5 veces** en cada apertura (medido).
- **Ahora (`src/lib/boot.js`, `runBoot`):** `authStore.restoreSession()` (local; deja la credencial cargada en el cliente de Supabase y `user`; sin sesión → `loading:false`) → `Promise.all([bootProfile(user.id), initializeApp()])` → `markRefreshed()` (sella `lastRefreshAt` para que el primer montaje de `RouteSync` no vuelva a pedir todo) → Inicio. `authStore.initialize()` se conserva (= restoreSession + bootProfile, en serie) por compatibilidad. El listener `onAuthStateChange` se registra una sola vez (`_authListenerBound`).
- **Medición real** (Chromium 390 px, build de prod servido por http, sesión guardada en localStorage con JWT de prueba, Supabase interceptado con 400 ms de latencia por pedido; `scratchpad/qa/boot-bench.mjs <puerto> <etiqueta>`): Inicio visible **1035–1040 ms → 626–633 ms** (un viaje menos); pedidos en el arranque: 9 tablas **3× → 1×**, ficha **5× → 2×**; **0 pedidos sin credencial** en ambas versiones; datos frescos en el primer pintado en ambas. Tests nuevos: `src/lib/boot.test.js` (paralelismo real, espera a ambas, sin sesión, error de ficha) y `src/stores/authStore.boot.test.js` (restoreSession/bootProfile/loading/initialize compat). 105 tests, lint 0 errores, build OK.
- **Trampas del arnés:** el `<link>` a Google Fonts del `index.html` es render-blocking y en el sandbox cuelga ~12 s por el proxy (idéntico en ambas versiones): cortarlo con `page.route(/fonts\.(googleapis|gstatic)/, abort)` para medir solo la app. `python -m http.server` sirve en serie: no comparar builds servidos por servidores distintos. La sesión de prueba va en `localStorage['sb-<ref>-auth-token']` con `expires_at` futuro (getSession no viaja); los pedidos `.single()` llevan `Accept: application/vnd.pgrst.object+json` → responder objeto o 406 PGRST116.

**Landmine nuevo:**
62. **Orden del arranque = `runBoot` en `src/lib/boot.js`, y nada más.** (a) NUNCA lanzar un pedido de datos antes de que `restoreSession()` haya resuelto: sin la credencial cargada, PostgREST responde como `anon` (filas vacías/parciales) y `initialize()` **cachearía eso en localStorage**. La paralelización es SOLO entre ficha y tablas, ambas después de la sesión. (b) La pantalla de carga espera `Promise.all` de las dos (landmine #61). (c) Después del boot se llama `markRefreshed()`; si agregás otro "refresh al montar", pasalo por el throttle de `RouteSync` o volvés a triplicar las cargas. (d) `loading` del authStore lo suelta `bootProfile` (con o sin error) o `restoreSession` cuando no hay sesión — si alguna rama no lo suelta, la app queda en "Cargando…" para siempre. (e) `refreshProfile()` sigue llamando `appStore.initialize()` (recarga completa): úsalo solo tras cambios de perfil, no como "refresco liviano".


## Estado al 2026-09-12 (III) — Multi-área: plan de canales + auditoría adversarial de toda la feature (PR #119, #120)

**Plan de canales (micrófonos) para Sonido — EN PROD (PR #119, verificado en vivo).** Tabla `order_channel_plans` (1 fila/orden, jsonb `{"<memberId>:<instrumento>": <nro canal>}`; `is_sonido_observer()` INVOKER; RLS write = pastor/líder/Sonido). `ChannelPlanModal` autonumera desde la formación con **color por instrumento** (`src/lib/channelPlan.js`, paleta aprobada por Paul) y números **editables por Sonido**, guardados por orden. Botón en el banner de Sonido y en el detalle de Órdenes.

**Auditoría adversarial de TODA la feature multi-área (Workflow 32 agentes, 4.1M tokens, 11 hallazgos confirmados) + remediaciones (PR #120, backend ya aplicado a prod + verificado, migración `20260912_post_audit_fixes_multi_area.sql`).**
- **DEDUP de correos/push CONFIRMADO CORRECTO** (requisito central de Paul): ningún camino manda 2 correos/push al mismo destinatario por el mismo evento — `SELECT DISTINCT m.id`, ramas `IF v_content … ELSE` mutuamente excluyentes, exclusión `NOT (m.id = ANY(v_notified))` en la rama de formación, y UN solo trigger de insert + UN solo trigger de update en `orders`. Un miembro Adoración+Sonido recibe 1 correo + 1 push por evento, ve **ambos banners** (Adoración arriba) y el welcome respeta Adoración-primero (`_member_push_area_label`).
- **MEDIUM corregido:** 8 órdenes (4 futuras, visibles a observadores) guardaban basura de UI (`_localId/_pendingHistory/_suggestedDirector`) en `orders.songs`. Ver landmine #63.
- **low corregidos:** `_ensamble_html` colgaba " a las " con `rehearsal_time` NULL/'' (`_html_escape` devuelve '' no NULL → se resolvió con `CASE WHEN NULLIF(p_time,'') IS NOT NULL …`); `order_channel_plans` → `REVOKE anon` + `FORCE ROW LEVEL SECURITY` (patrón landmine #45; inerte hoy porque RLS ya default-deny a anon y no hay policy {anon}, puro defense-in-depth); `src/lib/areas.js` `FORMATION_AREAS → ['multimedia','sonido']` (espejo del helper vivo `_area_formation_slugs`, landmine #58; estaba inerte porque no se consume, pero rompía el contrato de espejo); `ChannelPlanModal` — el input de canal ya no salta al autonumerado ni se reordena al vaciarlo (guarda el texto crudo en `drafts` y lo resuelve al blur; `handleSave` flushea drafts vía `resolveOverrides()` para no perder una edición sin blur).
- **Nota adversarial sobre el GRANT de anon:** tener anon en `relacl` es el **DEFAULT de Supabase en ~25 tablas**; la compuerta real es RLS. Solo son explotables las tablas con policy `{anon}`/`{public}` (devocionales/reflexiones/pending_registrations = flujos públicos intencionales). **NO hacer un REVOKE masivo** (rompería el registro público). Se blindó solo `order_channel_plans` (en scope); `band_temporary_members`/`practice_logs`/`member_activity`/`service_feedback` conservan el grant **inerte** (sweep opcional, ofrecido a Paul).
- **Decisiones de producto abiertas (ofrecidas a Paul, no bloqueantes):** (a) el **autor** de un orden recibe el correo de su propio orden en el ALTA, mientras que la edición sí excluye al editor (asimetría; 1 solo correo, no viola dedup); (b) una edición combinada contenido+formación manda el push/campanita con título genérico ("Orden actualizado") en vez del personal ("Te sumaron a la formación") — el correo igual lleva la línea personal por `_lineup_html` y los avisos de práctica se resuelven por `order_participant_ids`, así que nadie queda sub-notificado.

**Landmine nuevo:**
63. **Un trigger BEFORE que sella/avisa sobre una columna jsonb de contenido MISFIREA en el primer guardado limpio de filas históricas "sucias".** `orders.songs` guardaba basura de cliente (`_localId/_pendingHistory/_suggestedDirector`) en 8 órdenes previas a `stripSongRefs`. Como `convertOrderToDB` ahora corre `stripSongRefs`, el primer `updateOrder` del cliente (aunque fuera SOLO estado/feedback) escribía `songs` limpio ≠ el guardado con basura → `songs IS DISTINCT FROM` = true → disparaba el BEFORE `set_order_content_changed` (sella `content_changed_at` → banner "¡Ojo! Hubo cambios") **y** la rama de contenido de `notify_on_order_update` (correo+push `orden-editado` a banda ∪ pastores ∪ observadores), todo FALSO. Fix: limpiar la basura server-side con `set_order_content_changed` **y** `notify_order_update` DESHABILITADOS (para que la limpieza no misfire; corre como SQL → `auth.uid()` NULL → el notify igual se auto-corta, pero el BEFORE de sello NO tiene guard de auth → hay que deshabilitarlo). Verificar `next_save_is_noop` (jsonb del cliente == jsonb guardado). Al agregar un BEFORE trigger nuevo que reaccione a una columna jsonb, **barré la tabla por filas sucias históricas ANTES** de confiar en "solo dispara ante cambios reales". La limpieza preserva `songId/key/directorId` (remové solo las claves con prefijo `_`).

## Estado al 2026-09-12 (IV) — Suspender/Reactivar/Reprogramar ensamble (#121) + Salud de los ensambles (reporte trimestral)

**"Suspender ensamble" (PR #121, EN PROD, backend + cliente + verificado en vivo).** Diseñado con Workflow (relevamiento+diseño+red-team). Un pastor/líder (solo su banda) puede **Suspender / Reactivar (mismo día/hora) / Reprogramar (otro día/hora)** el ensamble de un orden, desde el detalle del orden y desde el card "¡Hoy tenés ensamble!". Modal con **motivo opcional** → **mail + push** a la **formación (order_participant_ids) ∪ pastores ∪ observadores de Sonido**. Estado "Ensamble suspendido" (rose + "ver más" del motivo) en 6 superficies (card, detalle, PrepBanner, banner Sonido `ensamble_suspendido`, pill del calendario). El servicio NO se cancela; la alarma de práctica sigue; **cancelar el orden arrastra** (el cron y el card filtran `status='scheduled'`). Migración `20260912_suspender_ensamble.sql`: 3 columnas server-owned `orders.rehearsal_suspended_at/reason/by`; trigger **INVOKER `enforce_order_rehearsal_rules`** (freeze + auto-clear al reprogramar); RPCs `suspend/resume/reschedule_order_rehearsal` (SECURITY DEFINER, gate pastor/líder-miembro-permanente, CAS atómico, fan-out best-effort, motivo escapado); `send_rehearsal_reminders` con `status='scheduled' AND rehearsal_suspended_at IS NULL` + re-chequeo in-loop; 3 plantillas `ensamble-suspendido/reactivado/reprogramado`. QA transaccional completa + advisors 0 nuevos + UI en vivo (botones, ambos modales, render suspendido con "ver más"). Cliente: converters (FromDB sí, ToDB no) + cloneOrder reset + store suspend/resume/reschedule (patch optimista) + `RehearsalActionModal`.

**"Salud de los ensambles" — reporte trimestral (EN PROD, 100% backend).** Migración `20260912_salud_ensambles_reporte.sql` + cron `ensamble-health-report` (jobid 17, `0 11 * * *` = 08:00 ART, se auto-gatea a cada 90 días desde el ancla **2026-10-01**). `send_ensamble_health_report(p_now, p_force)` manda a **pastores (todas las bandas) y líderes (su banda permanente)** un mail `salud-ensambles` con el % de **ensambles concretados** por banda en los últimos 90 días. **Concretado = `rehearsal_date IS NOT NULL AND rehearsal_suspended_at IS NULL`** (reprogramado/reactivado limpian la suspensión → cuentan; suspendido-y-abandonado NO; servicio sin ensamble tampoco). Denominador = servicios (órdenes no canceladas) de la banda en la ventana. Tramos (exhaustivos, % entero): ≤49 peligro · 50-70 disciplina · 71-90 bien · 91-100 excelente, cada uno con la frase de Paul + un versículo (Ecl 9:10 / Prov 12:24 / Col 3:23 / Mt 25:21). Dedup por día (`email_throttle` key `ensamble_health:<fecha>`). QA transaccional: pct por banda correcto (Sábado 2/4=50, Domingo 2/2=100), pastor todas / líder solo la suya, gating de 90 días, dedup. Advisors 0 nuevos (la función es service_role-only, no aparece en el WARN de authenticated). **El conteo ARRANCA el 1/10; el PRIMER reporte es el ~30/12/2026** (ancla+90), cubriendo `[1/10, 30/12)` — NO dispara el 1/10 (corrección de Paul: evitar cifras de un período previo a la función). La ventana se clampa con `GREATEST(hoy−90, ancla)`.

**Landmines nuevos:**
64. **Columnas de suspensión del ensamble = server-owned, congeladas por trigger, NO por REVOKE de columna.** `orders.rehearsal_suspended_at/reason/by` (+`rehearsal_reminder_sent`) las escribe SOLO el backend (RPCs/cron). Un `REVOKE UPDATE(col)` NO sirve mientras `authenticated` tenga el GRANT UPDATE de TABLA (regla #7): el privilegio de tabla cubre las columnas nuevas. El blindaje es el trigger BEFORE UPDATE **`enforce_order_rehearsal_rules` (SECURITY INVOKER**, exime backend por `rolbypassrls`, igual que #41): para el cliente, si cambió `rehearsal_date/time` → limpia la suspensión + re-arma el recordatorio (reprogramar desde el editor); si no → fuerza las 4 columnas a OLD (neutraliza cualquier PATCH directo, SIN error). Van en `convertOrderFromDB` (+ derivado `rehearsalSuspended`) pero NUNCA en `convertOrderToDB` (regla #8). Escribir la suspensión SIEMPRE por las RPCs `suspend/resume/reschedule_order_rehearsal` (CAS atómico `WHERE rehearsal_suspended_at IS NULL/IS NOT NULL` → solo el ganador notifica, mata el doble-aviso). Aviso del ENSAMBLE = formación ∪ pastores ∪ **Sonido** (Sonido opera en el ensamble; decisión de Paul), NO Multimedia. Suspender ≠ cancelar el orden; pero **cancelar el orden arrastra** (cron + card filtran `status='scheduled'`).
65. **`send_ensamble_health_report` se auto-gatea; NO poner el gate de 90 días en el cron.** El cron corre DIARIO (`0 11 * * *`); la función chequea `hoy_ART >= ancla+90 AND (hoy_ART − 2026-10-01) % 90 = 0` (salvo `p_force` para QA) y dedup por día. El `+90` = el 1/10 NO dispara (primer reporte ~30/12); la ventana se clampa con `GREATEST(hoy−90, ancla)` para no contar antes del 1/10. Si cambiás la cadencia, cambiá el ancla/módulo en la función, no el schedule del cron. **Concretado** se lee del estado FINAL de la columna `rehearsal_suspended_at` (para órdenes pasadas es estable). Para QA: `p_force:=true` salta el gate; sembrar órdenes con `notify_on_order_insert` deshabilitado (el rol del MCP NO puede `SET session_replication_role`) y envolver en `DO … RAISE` (revierte el DISABLE). La función es SECURITY DEFINER **service_role-only** (no authenticated) → no suma al advisor 0029.

## Estado al 2026-09-12 (V) — Auditoría FINAL adversarial de todo lo del día + remediaciones (#124 pendiente)

Antes de que Paul anuncie los cambios, Workflow de auditoría adversarial (21 agentes, 6 dimensiones + verificación independiente de cada hallazgo) sobre lo desplegado hoy (suspender/reactivar/reprogramar ensamble + salud + editor-mail). **14 hallazgos confirmados, 0 crítico / 0 alto** (3 medio, 8 bajo, 3 info). Remediados (migración `20260912_auditoria_final_fixes.sql`, aplicada a prod + QA transaccional):
- **#1/#4/#8/#11 (el más importante):** el freeze `enforce_order_rehearsal_rules` LIMPIABA la suspensión en silencio al cambiar `rehearsal_date/time` desde el editor/PATCH (incl. un líder sobre banda AJENA, porque la RLS de UPDATE de `orders` es `is_pastor_or_leader()` GLOBAL). Fix: si el ensamble está SUSPENDIDO y el cliente (no-backend) cambia la fecha/hora → **RAISE 42501** (obliga a usar las RPCs Reprogramar/Reactivar, que avisan). Reprogramar un orden NO suspendido desde el editor sigue igual. Verificado: editor-sobre-suspendido→42501, cross-band→42501, no-suspendido→OK.
- **#6:** `reschedule_order_rehearsal` sin CAS → doble-tap duplicaba el aviso. Fix: `WHERE ... AND (fecha/hora distinta OR había suspensión)` → el 2º idéntico da `ok:false`, 0 mails.
- **#7:** el nombre de banda en `{{banda_sufijo}}` de los correos de ensamble NO se escapaba (HTML-injection). Fix: variable del correo escapada (`_html_escape`); el push (texto plano) usa el sufijo plano. Verificado `<b>`→`&lt;b&gt;`.
- **#5:** `resume` ahora exige ensamble futuro (simétrico con suspend/reschedule). **#9:** filtro de salud `status IS DISTINCT FROM 'cancelled'` (NULL-safe). **#14:** plantilla salud `cta_text='Ver los órdenes'` (masculino, #26). **#3/#13 (cliente):** el pill del calendario y el bloque de detalle ya NO muestran el ensamble de un orden **cancelado** (cancelar arrastra).
- **INFO/no-accionable:** #10 (líder recibe el reporte por membresía PERMANENTE de `bands.members`, no por vínculo de liderazgo — es el diseño correcto: `leader_of` es texto libre desde #58); #12 (el gate `canManage` del cliente usa fecha LOCAL del dispositivo, no ART — inocuo para un ministerio 100% ART; la RPC es la frontera real).

**RECOMENDACIÓN pendiente (RAÍZ preexistente, NO de este deploy) para Paul:** la RLS `orders_update_pastor_or_leader` / `..._delete` es `is_pastor_or_leader()` GLOBAL → **cualquier líder puede editar/borrar por PATCH crudo el orden de CUALQUIER banda** (reescribir canciones, mover fecha del servicio, borrarlo). La UI lo gatea pero la base no. El fix de #1 tapó el síntoma nuevo (des-suspender ajeno); la raíz (acotar la RLS de UPDATE/DELETE de `orders` a la banda del líder — miembro permanente) es un cambio mayor de comportamiento preexistente y quedó OFRECIDO a Paul (no se toca sin su OK explícito, regla #2).

## Estado al 2026-09-12 (VI) — Detalle de Orden más pedagógico (servicio vs ensamble + colapsables) (#125/#126)

Pedido de Paul (UX/reorganización, sin cambios de lógica): diferenciar las DOS instancias del orden y hacerlo más intuitivo, sin scroll infinito (sobre todo en celu). Verificado en vivo (desktop + mock a 375 px).
- **Dos tarjetas APILADAS y COLAPSABLES** (formato horizontal full-width tipo "Practicar este orden", refinado en #128 tras pedido de Paul — antes eran lado a lado con alturas distintas, "desprolijo"): **"Día de servicio"** (fecha/hora + estado + banda + tipo → despliega *Marcar realizado / Cancelar servicio / Reabrir*) y **"Día de ensamble"** (fecha + estado → despliega *Suspender/Reactivar/Reprogramar* + motivo; empty-state estático si no hay ensamble). Cada una se despliega con sus opciones vía `CollapsibleSection` (header slot). Así queda claro que "realizado/cancelar" es del SERVICIO y "suspender/reprogramar" es del ENSAMBLE (`data-testid` detail-service / detail-rehearsal / detail-rehearsal-empty). Verificado en vivo (colapsan/despliegan, chevrons rotan).
- **Secciones colapsables** (Formación, Canciones —abierta por defecto—, Devolución) con el componente nuevo **`src/components/ui/CollapsibleSection.jsx`**: animación de altura prolija con el truco `grid-template-rows: 0fr↔1fr` + hijo `min-h-0 overflow-hidden` (sin librerías), respeta `prefers-reduced-motion`. Header con título + count + chevron que rota.
- **Card de la lista (móvil):** los chips de canciones/formación se abrevian a **ícono + número** con `hidden md:inline` en la palabra (evita el corte feo en pantallas chicas) + `title` de tooltip en compu.
- **Fecha de las tarjetas:** `first-letter:uppercase` (no `capitalize`, que ponía "De Septiembre De").

## Estado al 2026-09-12 (VII) — "Día de ensamble" siempre con sus opciones (aunque ya pasó) + banner del Inicio legible

Paul (capturas 14:14/14:16 ART, ya con #128 en el teléfono): (1) en el detalle del orden de Banda Martes 15/09, la tarjeta **"Día de ensamble"** (jueves 10/09, **ya pasado**) se veía como fila estática, sin chevron ni opciones; (2) en el Inicio, los botones **"Suspender ensamble / Reprogramar"** del banner dorado "¡Hoy tenés ensamble!" iban en `Button secondary` (letra gris clara + borde gris) → ilegibles sobre el dorado y partidos en dos líneas.
- **Causa de (1):** `canManage` en `Ordenes.jsx` exigía `rehearsalDate >= hoy` para las TRES acciones, y `CollapsibleSection` sin `children` se pinta como fila estática. Pero en la base solo `suspend_order_rehearsal`/`resume_order_rehearsal` exigen ensamble de hoy/futuro; `reschedule_order_rehearsal` exige únicamente que la fecha **NUEVA** no sea pasada. Fix: `canManage = (pastor | líder de la banda) && status='scheduled'`; `canToggle = canManage && !isPast` (Suspender/Reactivar); **Reprogramar siempre**; con ensamble vencido se muestra la nota *"Este ensamble ya pasó. Si hace falta, podés reprogramarlo para otro día."* (`detail-rehearsal-past`). El modal Reprogramar arranca con el día **vacío** si el actual ya pasó (`min` = hoy) para obligar a elegir uno nuevo.
- **(2):** las tres acciones del banner comparten el estilo de "Ver el orden" (tinta negra `bg-black/10 text-black` sobre dorado; `bg-white/10 text-white` sobre el rosa de suspendido), `flex-1` + `flex-wrap`, `text-xs`, íconos 13 px, `px-2`, etiquetas cortas **"Suspender" / "Reprogramar"**; el card pasa a `p-4 sm:p-5`. Medido en Chromium con la **fuente Inter real** (el arnés la sirve desde `scratchpad/qa/fonts`, paquete `@fontsource/inter`): contenido mínimo 301 px → **una sola línea a 375 y 390 px**; a 360 px la última baja de línea sin desbordar. `data-testid` `rehearsal-card` / `rehearsal-card-actions`.
- **QA:** `scratchpad/qa/ensamble-card.mjs` **33/33** (detalle con ensamble pasado / pasado+suspendido / futuro, por rol: pastor, líder de la banda, líder ajeno, miembro; RPC `reschedule` con la fecha nueva y luego aparece Suspender; banner a 390/375/360 con color computado negro y una línea; RPC `suspend` con motivo → estado suspendido en blanco; miembro solo "Ver el orden") + `run.mjs` **49/49** (arnés de formación adaptado a las secciones colapsables de #125). Lint 0 errores, 105 tests, build OK. El mock de Supabase del arnés ahora registra `rpc` en `window.__qa.calls` (`table:'rpc', op:<función>`).

**Landmine nuevo:**
66. **Los gates del ensamble en el cliente son el ESPEJO exacto de las RPCs.** Suspender/Reactivar: `status='scheduled'` **y** `rehearsal_date >= hoy` (la RPC devuelve `n=0` si no). Reprogramar: `status='scheduled'` y **solo la fecha NUEVA ≥ hoy** (el ensamble viejo puede haber pasado). Si escondés Reprogramar por fecha vencida, un orden futuro con ensamble pasado queda sin salida (fila estática, lo que reportó Paul); si mostrás Suspender para un ensamble pasado, el modal termina en "No se pudo completar". Sobre el card **dorado** del Inicio nunca uses `Button variant="secondary"` (gris sobre dorado): tinta negra `bg-black/10 text-black` como "Ver el orden". Para medir si los botones entran en una línea, cargá la fuente Inter real en el arnés (la fallback del sandbox mide distinto).

## Estado al 2026-09-12 (VIII) — "Algo salió mal" tras cada publicación: chunks viejos → recuperación sola + pantalla amable

**Reporte de Paul (14:42 ART, captura "Algo salió mal · La pantalla no pudo renderizarse…")** con pedido de un mensaje menos trágico ("es probable que haya una versión nueva… recargá… si vuelve a fallar, contactate"). **Causa real (verificada en `error_log`):** las 12 filas del día (4 usuarios: Paul, Yessica, Eze, Melanie; 6 episodios, todos iPhone) dicen `Importing a module script failed.` = **chunk viejo tras un deploy**. Vite emite cada página como `assets/<Página>-<hash>.js`; al publicar, los hashes cambian y Vercel deja de servir los viejos. Un teléfono con la app YA abierta (bundle anterior en memoria) que toca una sección que todavía no había descargado (Bandas, Repertorio, Mi Ensayo…) pide el hash viejo → 404 → el `import()` de `React.lazy` rechaza → ErrorBoundary raíz. Con 12 publicaciones en el día pasó 6 veces. No es un bug de ninguna pantalla: es la mecánica de publicar con la app abierta.
- **Fix de raíz (`src/lib/chunkRecovery.js`):** `isChunkLoadError(err)` (patrones Safari/Chrome/Firefox + `vite:preloadError` CSS) y `recoverFromStaleChunk()`: marca `sessionStorage['adorapp:chunk-retry']` (TTL 60 s, anti-loop), `markUpdateStep('reloading')` y `location.reload()` **una sola vez**. La recarga trae el `index.html` nuevo (hashes nuevos) y, como cambió `VITE_BUILD_ID` + hay marca `reloading`, la primera carga muestra sola **"Actualizando a la nueva versión"**. Enganches: `lazyPage` de `App.jsx` va envuelto en `withChunkRecovery` (deja el `<Suspense>` en "Cargando…" mientras recarga, sin parpadeo de error); `installChunkRecovery()` en `main.jsx` escucha `vite:preloadError`; los `import()` dinámicos de handlers (jsPDF en Órdenes/Repertorio, `docx` en el banner de Multimedia) hacen `if (isChunkLoadError(err) && recoverFromStaleChunk()) return;` antes de su modal de error.
- **ErrorBoundary (copy de Paul):** si el error es de chunk → **"¡Hay una versión nueva!"** + "El Pastor del área acaba de publicar cambios… Tocá «Actualizar la app»… Si vuelve a pasar, escribile a un pastor…" (botón "Actualizar la app"); cualquier otro error → **"Necesitamos recargar la app"** + "Es probable que haya una versión nueva con cambios que acaba de incorporar el Pastor del área. Recargá la app; si vuelve a fallar, contactate con un pastor para que podamos resolverlo a la brevedad." Ícono ✨ dorado (ya no el triángulo rojo). `componentDidCatch` reporta a `error_log` con `context.kind = 'stale-chunk' | 'render'` (para distinguirlos en la tabla) y, si es chunk, intenta la recuperación. El botón hace `markRetryNow()` + `reloading` + reload: la recarga manual **cuenta como el único reintento** (si la versión nueva sigue rota, vuelve la pantalla sin recargar dos veces). `data-testid="error-boundary"` + `data-kind`.
- **Hipótesis cubiertas además del caso reportado (canon de "todas las hipótesis", exigido por Paul):** (a) **deploy en pleno arranque** (el `index.html` viejo ya se sirvió y el script/CSS de entrada con hash viejo da 404 → sin React nadie lo atrapa → app en negro): script **inline en `index.html`** que escucha `error` en captura sobre `<script>`/`<link rel=stylesheet>` de `/assets/` y recarga una vez (misma marca anti-loop + marca `reloading`); (b) **sin conexión** (el `import()` falla por red, no por versión): `recoverFromStaleChunk` NO recarga si `navigator.onLine === false` y el boundary muestra **"Sin conexión"** + "Reintentar" (severity `warning`, `kind='offline-chunk'`); (c) **dependencias precargadas** (CSS/JS compartidos de una página) → `vite:preloadError`; (d) **error colateral durante la recarga** (Vite resuelve `undefined` con el evento cancelado → `TypeError … reading 'Bandas'`) → `reloadPending` lo deja pendiente y el boundary no lo reporta; (e) **doble recarga desde el botón** → la recarga manual cuenta como el único reintento; (f) **`import()` en handlers** (jsPDF, docx) → guard; (g) **`import('../lib/supabase')`** de Login/Miembros apunta al bundle de entrada ya cargado (sin red) → no aplica; (h) **HTML por chunk faltante** (rewrite SPA) → `vercel.json` excluye `assets/.*` del rewrite → 404 real, y el SW solo cachea `fresh.ok`; (i) **`index.html` cacheado por el navegador** → Vercel lo sirve `max-age=0, must-revalidate` (solo `/assets/*` es immutable); (j) **pestañas múltiples** → marca por pestaña (`sessionStorage`); (k) **observabilidad**: cada recuperación deja `sessionStorage['adorapp:chunk-recovered']` y la carga siguiente la reporta a `error_log` como **`info`** (`kind='stale-chunk-recovered'`, `via='lazy'|'entry'`, `fromBuild/toBuild/path/asset`) para ver cuántas veces pasa sin que cuente como error de nadie. Descartadas con evidencia: `beforeunload` (no hay handlers que frenen la recarga); otros `React.lazy` (solo `App.jsx`); `sw.js` NO se toca (cambiarlo dispara la doble carga a todos los usuarios). Trampa del arnés `scratchpad/qa` cazada de paso: `chunkRecovery` → `errorReporter` importa `'./supabase'` (hermano en `src/lib`) y el plugin `resolveId` solo mapeaba `lib/supabase` → se cargaba el `supabase.js` REAL además del mock y `window.__qa.calls` quedaba vacío; el plugin ahora mapea también `./supabase` desde `src/lib/*` al mismo id del mock (una sola instancia).
- **QA real (`scratchpad/qa/chunk-recovery.mjs` + `static-server.mjs` con `POST /__swap?dir=[&after=N]`):** dos builds de prod con `VERCEL_GIT_COMMIT_SHA` distintos (A y B) + C = B sin `assets/Bandas-*.js`. Chromium 390 px con sesión falsa y Supabase interceptado: **A abierta → se publica B → tocar Bandas**: el servidor registra el 404 real del chunk viejo, la app recarga exactamente una vez, muestra "Actualizando a la nueva versión", entra a Bandas, `adorapp:build` pasa de A a B, sin pantalla de error. **A → C (rota)**: una sola recarga y la pantalla amable "¡Hay una versión nueva!" (sin loop: los loads no siguen creciendo); el botón reintenta exactamente una vez; con B sana publicada, el botón entra a Bandas. **C) deploy en pleno arranque** (`swap(B, after=1)`: el `index.html` sale de A y el script de entrada ya no existe): el 404 real del `index-*.js`, una recarga por el script inline, "Actualizando…", entra al Inicio, constancia `via='entry'`. **D) sin conexión** (`context.setOffline(true)`): no recarga, pantalla "Sin conexión" + Reintentar; al volver la red y con B publicada, Reintentar entra a Bandas. Unit: `chunkRecovery.test.js` (13) + `ErrorBoundary.test.jsx` (7, testing-library; en jsdom `location.reload` no se puede espiar → se reemplaza `window.location` por un doble con `Object.defineProperty`).

**Landmine nuevo:**
67. **Publicar con la app abierta rompe los `import()` de páginas no visitadas (hash viejo → 404).** Toda carga perezosa de código (páginas de `App.jsx` vía `lazyPage`, y los `import()` dinámicos de jsPDF/docx en handlers) DEBE pasar por `src/lib/chunkRecovery.js`: `withChunkRecovery` para `React.lazy`, o `if (isChunkLoadError(err) && recoverFromStaleChunk()) return;` en el `catch` de un handler. Si agregás una ruta lazy nueva, usá `lazyPage` (ya envuelto); si agregás un `import()` en un handler, ponele el guard antes del modal de error. La recuperación es **una** recarga por ventana de 60 s (`sessionStorage['adorapp:chunk-retry']`): no la quites ni la hagas "hasta que funcione" (sin red sería un loop infinito). El `ErrorBoundary` distingue `stale` (copy "¡Hay una versión nueva!") de `generic` (copy "Necesitamos recargar la app") y NUNCA debe volver a decir "Algo salió mal" / rojo (pedido de Paul). En `error_log`, `context.kind='stale-chunk'` identifica estos casos: no son bugs de pantalla, son deploys con usuarios adentro; si se repiten fuera de días de deploy, revisar el SW (`sw.js` cachea `/assets/*` cache-first). Para reproducirlo: dos builds con `VERCEL_GIT_COMMIT_SHA` distintos servidos por `scratchpad/qa/static-server.mjs` y swap en caliente (no alcanza con borrar un archivo: el 404 tiene que ocurrir en un `import()` de una navegación en-app). El script **inline de `index.html`** es la única red para el arranque (sin React): si tocás el `<head>`, conservalo, y mantené sus claves de `sessionStorage` (`adorapp:chunk-retry`, `adorapp:update`, `adorapp:chunk-recovered`) en sincronía con `chunkRecovery.js`/`updateProgress.js`. Sin conexión NUNCA se recarga solo (sería un loop contra el SW); el boundary lo explica como "Sin conexión".

## Estado al 2026-09-12 (IX) — "¿Cómo estuvimos?" SOLO para el líder de la banda, 48 h desde la hora del servicio

**Reporte de Paul:** Gustavo Godoy (rol `member`, integrante de Banda Domingo) tenía la tarjeta "¿Cómo estuvimos? · Dar mi feedback" en su Inicio. Regla definitiva de Paul: la tarjeta se le ofrece **solo al líder de la banda que tuvo el servicio**, **durante las 48 horas siguientes a la hora del servicio**, y después **desaparece sola**.
- **Causa:** `resolveFeedbackOrder` (dentro de `ServiceFeedbackPrompt.jsx`) chequeaba `pastor || integra la banda efectiva` y **nunca el rol `leader`** → cualquier integrante de la banda veía la tarjeta. Nadie llegó a enviar (tabla `service_feedback` vacía; la Edge Function `send-service-feedback` sí rechazaba a los miembros con 403 → sin correos indebidos). Además la ventana era "≥ 4 h después del servicio y hasta 14 días" y no se re-evaluaba con la pantalla abierta.
- **Fix:** regla pura en **`src/lib/serviceFeedback.js`** (`serviceStartEpoch` ART→UTC, `feedbackWindow` = [inicio, inicio + 48 h), `isBandLeader` = rol `leader` + miembro **permanente** de `bands.members`, `resolveFeedbackOrder` elige el inicio más reciente en ventana) + 12 tests. La tarjeta re-evalúa con un `setTimeout` al vencimiento (desaparece sola sin recargar). **Edge Function `send-service-feedback` (redeployada):** ahora solo `leader` de la banda (ya NO pastores) y ventana [inicio, +48 h) con el mismo cálculo; orden cancelado → 400; fuera de ventana → `ventana_vencida` (el cliente lo explica). Sin cambios de tabla/RLS.
- **Decisión que queda a confirmar por Paul:** al tomar literal "solo el líder", los **pastores ya no ven ni pueden enviar** esta devolución (antes sí: "pastor, cualquier banda"). Reincorporarlos es una línea en `isBandLeader`/EF si Paul lo quiere.
- **QA:** Chromium 390 px sobre el Dashboard real con fixtures relativas al reloj (`o-fb` empezó hace 2 h; `o-fb-exp` vence en ~12 s; `o-fb-future` en +90 min): miembro (Gustavo) NO ve la tarjeta; líder de la banda SÍ; pastora NO; líder de otra banda NO; líder **temporal** de la banda NO; el servicio futuro NO se ofrece; la tarjeta **desaparece sola** al vencer la ventana con la pantalla abierta. Regresión formación 49/49 y ensamble 33/33.

**Landmine nuevo:**
68. **Toda oferta de acción "para líderes" en el cliente DEBE chequear el ROL, no solo la pertenencia a la banda.** `integra la banda` ≠ `es el líder` (Gustavo es miembro de dos bandas). Fuente única de la regla del feedback: `src/lib/serviceFeedback.js` (cliente) espejada en la EF `send-service-feedback` (frontera real). Ventana = **48 h desde la hora del servicio** (inclusive el inicio; a las 48 h exactas se va); pertenencia = **permanente** (`bands.members`, no temporales, igual que `canManageRehearsal`). Si cambiás la ventana o el rol, cambialo en los DOS lados y en los tests. **Pastores reincorporados por decisión de Paul (Estado XI):** `canGiveFeedback` = pastor (cualquier banda) o líder de la banda, en el cliente y en la EF.

## Estado al 2026-09-12 (X) — Auditoría transversal de ROLES y PERMISOS (cliente + servidor + red-team)

Pedido de Paul tras el caso del feedback: "revisá todo el tema de roles y permisos, que nadie vea nada incorrecto", con el canon de todas las hipótesis. Método: 3 auditores en paralelo (barrido del cliente gate por gate con matriz por rol; barrido del servidor: `pg_policies`/`pg_proc`/`pg_trigger`/storage/EFs; red-team empírico impersonando pastor, líder, miembro, editor, observadores, inactivo y anon en `BEGIN…ROLLBACK` con triggers de aviso apagados) + verificación propia de cada hallazgo + QA transaccional post-fix (Q1–Q10, todo revertido).

**Corregido en esta tanda (migración `20260912_auditoria_roles_permisos.sql`, aplicada a prod; 4 EFs redeployadas; cliente en la misma PR):**
- **Cliente:** el PDF "Exportar" del orden imprimía la **"Devolución del Pastor"** a cualquier rol (en pantalla ya era solo pastor) → solo pastor. Un miembro **editor** veía "Eliminar canción" (la RLS lo bloqueaba y la app mentía "eliminada") → Eliminar solo pastor/líder y `deleteSong/deleteBand/deleteOrder` usan `count: 'exact'` (0 filas = `false`, no mentir). Búsqueda por **email** en Miembros solo pastor. `Solicitudes`/`Comunicaciones`/`Repertorio` usan `useCurrentRole()` (una sola fuente de verdad). **Cuenta desactivada con sesión viva**: `Layout` cierra la sesión y `Login` explica "Tu cuenta está desactivada". **"Practicar este orden" / `/practica/:id`** solo banda efectiva del orden + pastor (antes cualquiera podía "practicar" cualquier orden). `authStore.signUp` (código muerto que escribía `members` desde el cliente) eliminado.
- **Servidor:** `notifications` sin UPDATE/DELETE para el cliente (un miembro podía volver GLOBAL una notificación propia con texto arbitrario — la policy no tenía WITH CHECK). Storage `avatars`: INSERT solo con prefijo propio (member id o auth uid), UPDATE/DELETE solo del dueño (`owner_id`) — antes cualquiera sobrescribía/borraba la foto de otro. `bands`: un líder ya **no puede sumarse a sí mismo** a una banda (con eso pasaba a "líder de esa banda" para ensamble/feedback/avisos: cadena de abuso confirmada por el red-team). `members`: `leader_of` y `pastor_area` **descongelados** (son personales; el perfil los edita y daba 42501), `email` **congelado** (va solo por la EF). **Lectura solo con ficha activa**: `members/orders/songs/bands/band_temporary_members/song_key_history/order_channel_plans` exigen `auth_role() IS NOT NULL` (en `members` además la fila propia); `communications` solo pastor (el cliente nunca la lee; la campanita usa `communication_notifications`). `pending_registrations`: trigger de saneo (control chars, largos, email en minúscula), WITH CHECK (solo `pending` sin aprobaciones/rol) y nombre **escapado** en el correo `registro-pendiente` (era HTML crudo + inyección de cabeceras vía `To:`). `banda_sufijo` escapado en `notify_on_order_insert/_update`. Hora `HH:MM` válida (CHECK en `orders.time/rehearsal_time` + regex del RPC). **RPCs de ensamble NULL-safe** (`COALESCE(_can_manage…, false)`: un inactivo con JWT podía suspender/reprogramar cualquier ensamble con fan-out masivo). Autoría firmada por el servidor (`order_channel_plans.updated_by`, `service_schemas/schema_templates.created_by` = `my_member_id()`).
- **EFs:** `admin-update-member` banea la cuenta de auth + revoca sesiones al desactivar (y desbanea al reactivar); el cliente desactiva/activa SOLO por esa EF. `send-push`: la ruta con JWT queda solo para pastor activo (un líder podía mandar un push arbitrario a TODOS). `send-emails`: cabeceras sin CR/LF (inyección) y RFC 2047 si hace falta. `collab`: banda/voluntario/categorías escapados en los correos.

**Decisiones de producto que quedaron para Paul (resueltas en Estado XI: D1 se mantiene, D2/D3/D5/D6 implementadas, D4 postergada):** (D1) el líder sigue pudiendo editar/borrar/crear **órdenes de OTRA banda**, escribir esquemas y planes de canales de cualquier orden y agregar integrantes a cualquier banda (RLS `is_pastor_or_leader()` global; recomendación de acotar por banda ya ofrecida en Estado (V)); (D2) `members` expone email/teléfono/cumpleaños de todos a cualquier autenticado por API/caché local (la UI los oculta a no-pastores) → vista `members_directory` sin datos de contacto para no-pastores (requiere cambio de cliente); (D3) los **pastores** ya no reciben la tarjeta "¿Cómo estuvimos?" (regla literal "solo el líder"); (D4) `admin-update-member` permite que uno cambie su propio correo de login sin verificación; (D5) columnas `password_hash` vacías en `members`/`pending_registrations` (borrar); (D6) `log-error`/`record-health-check` anónimas sin rate limit/secreto (bajo impacto).

**Landmines nuevos:**
69. **Lectura de datos = ficha ACTIVA.** Las policies de SELECT de `members/orders/songs/bands/band_temporary_members/song_key_history/order_channel_plans` exigen `(SELECT public.auth_role()) IS NOT NULL` (y `members` deja ver la fila propia). Al crear una tabla nueva de datos del ministerio, usar el mismo `USING`, NO `true`. `auth_role()` requiere `members.user_id` cargado y `active=true`: una ficha activa con `user_id` NULL quedaría a ciegas (hoy hay 0). Desactivar = EF `admin-update-member` (banea auth + revoca sesiones); nunca un `update({active:false})` directo desde el cliente.
70. **Todo dato de usuario que entre a un correo va por `_html_escape`** (las plantillas renderizan `cuerpo_html` RAW) **y ningún valor de cabecera puede llevar CR/LF** (`send-emails` los quita; `pending_registrations` los sanea en BEFORE INSERT). Puntos ya escapados: nombre del solicitante, `banda_sufijo` de alta/edición de orden, motivo/banda de ensamble, digest, formación, colab (banda/voluntario/categorías). Si agregás una plantilla o variable nueva, escapala en el origen.
71. **Gates de "líder de la banda" dependen de `bands.members` permanente:** el trigger `enforce_band_update_rules` impide que un líder se agregue a sí mismo (lo suma el pastor). No relajarlo: reabre la cadena "me sumo → suspendo/reprogramo/feedback de esa banda". Los RPCs de ensamble usan `COALESCE(_can_manage_order_rehearsal(), false)`: cualquier guard nuevo con helpers que puedan devolver NULL (inactivo → `auth_role()` NULL) DEBE ir con `COALESCE(…, false)` — `IF NOT NULL` no rechaza y un SECURITY DEFINER bypassa la RLS.
72. **Storage `avatars`:** INSERT solo `avatars/<mi member id>-…` o `avatars/<mi auth uid>-…`; UPDATE/DELETE solo `owner_id = auth.uid()`. Si cambiás el nombre de archivo del cropper (Header/MobileNav), actualizá la policy o la subida falla con 42501. `notifications` no acepta UPDATE/DELETE del cliente (marcar leído es en `notifications_read`).

## Estado al 2026-09-13 — Decisiones de Paul sobre la auditoría de roles (D2/D3/D5/D6)

Respuesta de Paul a las decisiones de Estado (X): **D1 se mantiene** (líderes sobre otras bandas), **D2** "¿por qué recibiría toda la info de otro miembro?" → resolver, **D3** "los pastores tienen que poder hacer y recibir todo, esa es la regla" → reincorporar, **D4** postergada, **D5/D6** resolver. Todo verificado con QA transaccional (rollback) impersonando miembro/líder/pastor/anon, 141 tests, build, y arneses Chromium (feedback 9/9 con Ana viendo la tarjeta, roles 12/12, formación 49/49, ensamble 33/33).
- **D3 · Pastores en "¿Cómo estuvimos?":** `src/lib/serviceFeedback.js` → `canGiveFeedback(band, member, role)` = pastor (cualquier banda) **o** líder permanente de la banda; misma ventana de 48 h. EF `send-service-feedback` redeployada con el mismo gate (`caller.role === 'pastor' || isLeaderOfBand`).
- **D2 · Datos personales solo para el pastor (y la propia ficha):** migración `20260913_decisiones_auditoria.sql` crea `member_private_fields(uuid)` (SECURITY DEFINER: correo/teléfono/cumpleaños de UNA ficha solo si `is_pastor()` o es la propia) y la vista **`members_directory`** (`security_invoker = true`: la RLS de `members` sigue decidiendo las filas; las tres columnas personales vienen NULL para quien no es pastor). Migración hermana `20260913_members_revoke_select.sql` (aplicada DESPUÉS del deploy del cliente): `REVOKE SELECT ON members` + `GRANT SELECT (columnas no personales)` → `select=*` sobre la tabla da 42501 desde la API. Cliente: `initialize()` y `fetchProfile()` leen la vista; `updateMember` hace el UPDATE sin `RETURNING` y relee la ficha desde la vista; realtime de `members` llega sin esas columnas (walrus respeta privilegios de columna) → `mergeMemberRealtimeRow` conserva lo que había y `refetchMemberFromDirectory` trae el dato fresco. Aclaración para Paul: ningún correo mandaba datos ajenos; el hueco era la API cruda y la copia local (`appMembers`) de cada teléfono, que ahora solo guarda datos personales propios (y todos, si es pastor).
- **D5 · `password_hash` eliminada** de `members` y `pending_registrations` (estaban vacías; primero se reescribió `enforce_member_update_rules` sin esa columna y las dos políticas de INSERT de solicitudes sin `password_hash IS NULL`).
- **D6 · EFs anónimas:** `log-error` → tope de `context` (8 KB serializado, recorte marcado), cuerpo ≤ 64 KB, freno 30/min por IP y 300/min por isolate (429) + trigger `rate_limit_error_log` en la base (300/min global, PT429). `record-health-check` → exige `Authorization: Bearer <clave del proyecto>` (el runner de `uptime.yml` ya manda la anon key; un POST cualquiera queda en 401). **Trampa cazada en vivo:** en el runtime de las EFs `SUPABASE_ANON_KEY` es la clave publishable nueva (`sb_publishable_…`), NO la anon key legacy (JWT) que manda el runner → comparar solo contra el env daba 401 al ping real; la EF valida la clave desconocida con `GET /auth/v1/settings` + `apikey` (200 solo para la anon/publishable de este proyecto; `/rest/v1/` no sirve porque hoy exige service_role), payload estricto (endpoint solo `adorapp.net.ar`, códigos/tiempos acotados, mensaje ≤ 500 sin control chars), freno 10/min + trigger `rate_limit_health_checks` (30/min). `REVOKE INSERT/UPDATE/DELETE ON health_checks` y `REVOKE INSERT/DELETE ON error_log` para anon/authenticated (el cliente nunca escribía directo; `error_log` conserva UPDATE para que el pastor marque resueltos). Un secreto compartido de verdad para el ping requeriría crear un secret en GitHub y otro en Supabase (no hay MCP para ninguno): queda como mejora si Paul lo pide.

**Landmine nuevo:**
73. **`members` NO se lee de la tabla desde el cliente: se lee la vista `members_directory`.** La tabla tiene `SELECT` de columna solo para las no personales (`id,name,role,instruments,active,user_id,avatar_url,created_at,updated_at,pastor_area,leader_of,editor,onboarded,areas`); `email/phone/birthdate` salen SOLO por la vista, y con valor solo para el pastor y la propia ficha (`member_private_fields`). Consecuencias: (a) cualquier `from('members').select(...)` nuevo que pida `*` o una columna personal falla con 42501 → usar `from('members_directory')`; (b) un `update(...)` sobre `members` NUNCA con `.select()` (RETURNING * pide las columnas revocadas): hacer el UPDATE y releer la vista (patrón `updateMember`); (c) **columna nueva en `members` = tres lugares**: `GRANT SELECT (col) ON members TO authenticated` (migración), la lista de columnas de la vista `members_directory` (recrearla) y los converters; si te olvidás del GRANT, el cliente no la ve y no hay error; (d) los eventos realtime de `members` NO traen las columnas personales para nadie (walrus respeta `has_column_privilege`) → el fundido del store conserva lo previo y relee la vista; no volver a `spec.from(newRow)` a secas para `members`; (e) las EFs y crons (service_role/postgres) no se ven afectados; (f) el cliente VIEJO (app abierta antes del deploy) cae al caché local en `initialize()` hasta que recarga: por eso el REVOKE va en una migración aparte que se aplica DESPUÉS de que Vercel esté READY. Para QA con `RETURNING`/`select` dentro de un `INSERT` como anon en `pending_registrations`: la fila también tiene que pasar la policy de SELECT (que anon no tiene) → probar SIN `RETURNING`, como hace `Login.jsx`.

## Estado al 2026-09-13 (II) — "¿Con qué ministramos?" (canción de ministración) + Ensayómetro para quien solo canta

Dos propuestas de Paul, implementadas juntas (PR `feat/ministracion-y-ensayometro-cantantes`). Decisiones de Paul: etiqueta "Ministración" también en PDF y presentador; el atajo vive SOLO en el banner (el editor de órdenes sigue igual y sus canciones no llevan la etiqueta); la hora del servicio es obligatoria; cantante = solo Voz/Coros; quien no tiene instrumento cargado se mide como músico (reporte enviado: Bautista Barua, Damaris y Tomás Ramírez estaban sin instrumento).

**"¿Con qué ministramos?"** — banner del Inicio (arriba de todo) visible SOLO para el líder permanente de la banda del orden y los pastores, durante un servicio EN EJECUCIÓN: orden `scheduled` con banda, desde la hora de inicio y por **3 h** (se auto-oculta al vencer, sin recargar, como la tarjeta de feedback). Atajo: buscador del repertorio → director (integrantes efectivos con 'Voz', misma regla que el editor) sugerido por historial + tono (memoria `song_key_history` del director si existe) → "Asignar y avisar". Una o más canciones por envío.
- **Regla pura:** `src/lib/ministration.js` (`resolveMinistrationOrder`, `ministrationWindow` = [inicio, +3 h), `canOfferMinistration` = pastor o `isBandLeader`, `isMinistrationSong`) + tests. Componente `src/components/dashboard/MinistrationBanner.jsx` (banner + modal). Store: `addMinistrationSongs(orderId, songs)` (RPC + parche optimista + `refetchOrder`).
- **Frontera real: RPC `add_ministration_songs(p_order_id, p_songs)`** (SECURITY DEFINER, GRANT authenticated; migración `20260913_ministracion_y_ensayometro.sql`): gate NULL-safe (pastor activo o líder activo miembro PERMANENTE de la banda), orden `scheduled` con banda, ventana [inicio, +3 h) calculada con `_service_start_utc(date, time)` (ART→UTC), 1..10 canciones con `songId` existente, tono `^[A-G]#?m?$`, director opcional = integrante EFECTIVO activo de la banda; **idempotente** (una canción ya marcada en el orden se saltea → doble tap seguro). Agrega al FINAL de `orders.songs` con `"ministracion": true`, sella `songs.last_used`, registra `song_key_history` del director y avisa (push `notifications` type 'order' + correo plantilla **`ministracion`**) a **formación (`order_participant_ids`) ∪ pastores ∪ Multimedia ∪ Sonido, sin quien lo hizo**. Errores con código legible (`ventana_vencida`, `todavia_no_empezo`, `orden_no_programado`, `director_invalido`, `tono_invalido`, …) que el modal traduce.
- **Sin doble aviso:** el UPDATE de la RPC dispararía también el aviso genérico "Orden actualizado" (`notify_on_order_update`, rama de contenido). Se silencia **ocupando su throttle** `email_throttle` key `order_edit:<id>` (90 s) ANTES del UPDATE, sin tocar esa función. QA: +15 avisos propios, 0 genéricos.
- **Etiqueta "Ministración":** detalle del orden (Badge), PDF del orden (texto junto al título), presentador `IniciarServicio` y Mi Ensayo. `stripSongRefs` conserva la clave (no lleva `_`); el editor precarga `{...s}` → editar el orden NO pierde la marca; `addSongToOrder` del editor NO la pone.
- **Hora obligatoria:** `orders.time` → `NOT NULL` (había 0 filas sin hora; default '20:00' se mantiene) + validación en `handleSubmit` de Órdenes (ErrorModal "Falta la hora").

**Ensayómetro para quien solo canta:** si TODOS los instrumentos de la persona **en ese orden** (`lineupInstrumentsFor`: formación cerrada → los de la formación; si no, los de su ficha) son Voz/Coros y tiene al menos uno, cada canción mide **3 hitos** (pasada + letra + estructura): "Frases y arreglos" no se muestra ni cuenta (aunque esté marcado de antes). Con cualquier instrumento además, 4 hitos. Fuente única `src/lib/ensayometro.js` (`isSingerOnly`, `milestonesPerSong`, `milestonesOf(log, singer)`, `ensayometroPercent(ids, logs, singer)`) usada por Mi Ensayo, el banner de preparación y **espejada en SQL** (`_singer_only(text[])` + `send_practice_reminders` con `_lineup_member_instruments(o.lineup, member)`; grants re-asertados, landmine #39).

**QA:** RPC 22/22 transaccional en prod (rollback; miembro/líder ajeno/anon/JWT sin ficha → 42501; entradas hostiles; caso feliz con avisos, historial de tono y `last_used`; idempotencia; pastor; ventanas futuro/vencida/completado); cron 5/5 (cantante al 100 % sin frases → sin alarma; músico → alarma; Voz+Piano en formación → 4 hitos; formación solo Voz manda sobre la ficha). Chromium `scratchpad/qa/ministracion.mjs` 21/21 (quién ve el banner, orden del banner, búsqueda, director/tono por SelectMenu, RPC con payload exacto, éxito, "Agregar otra", etiqueta en detalle y Mi Ensayo, desaparece solo, checks de cantante vs músico) + regresión feedback/roles/formación/ensamble. Lint 0 errores, 152 tests, build OK.

**Pastor multiárea (mismo día, PR `fix/pastor-multiarea-banners`):** regla de Paul: "el que tiene más de un área ve todo lo que compete a cada área; como pastor multiárea, todos los cards de todas las áreas". Los banners de área del Inicio salían solo si la ficha tenía el área (Paul y Ana tienen `areas={}`). Nuevo `effectiveAreas(member, role)` en `src/lib/areas.js` (pastor → sus áreas ∪ TODAS las observadoras; el resto, las de su ficha) usado por `ObserverAreaBanners` (prop `role`). Quien tiene dos áreas ya veía un banner por área (Rodrigo Pineda y Bautista Barua: Multimedia+Sonido). Corrección al reporte de "sin instrumento": Bautista Barua y Tomás Ramírez son observadores (Multimedia/Sonido), no necesitan instrumento; la única de Adoración sin instrumento es Damaris. QA `scratchpad/qa/areas-pastor.mjs` 6/6 + tests `areas.test.js`.

**Landmines nuevos:**
74. **La ministración se escribe SOLO por la RPC `add_ministration_songs`** (nunca `updateOrder` con la marca desde el cliente: no avisaría a las áreas ni al orden correcto, y sí dispararía el aviso genérico). La RPC silencia el aviso genérico ocupando el throttle `order_edit:<id>` (90 s): si alguna vez cambiás la clave o la ventana del throttle en `notify_on_order_update`, cambialas también en la RPC o volverán los avisos dobles. La marca es la clave `ministracion: true` en `orders.songs[]` (sin `_`, para que `stripSongRefs` la conserve); el editor de órdenes la preserva porque copia `{...s}`. La ventana (3 h desde `date`+`time` ART) y el gate (pastor / líder permanente) viven en `src/lib/ministration.js` **y** en la RPC: cambiarlos en los dos lados y en los tests.
75. **Hitos del Ensayómetro dependen de los instrumentos EN ESE ORDEN.** `isSingerOnly(lineupInstrumentsFor(order, member))` (cliente) ≡ `_singer_only(_lineup_member_instruments(lineup, member))` (SQL): solo Voz/Coros → 3 hitos, si no 4. Si agregás una categoría "de canto" nueva, sumala a `SINGER_INSTRUMENTS` (JS) **y** al literal de `_singer_only` (SQL). `orders.time` es NOT NULL: cualquier alta/edición de orden sin hora falla en la base (el form la exige antes).
76. **El pastor es multiárea por ROL, no por ficha:** cualquier gate de cliente por área (`memberAreas(...).includes(...)`) debe pasar por `effectiveAreas(member, role)` o sumar `isPastor` explícito; en `Ordenes.jsx` el pastor ya entra por `isPastor`. Los banners de área reciben `role`. En SQL los pastores ya reciben todo (avisos por `role='pastor'`), no hay espejo que cambiar.
